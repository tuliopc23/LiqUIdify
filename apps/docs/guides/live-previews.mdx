---
title: "Live Component Previews"
description: "Interactive documentation system for Liquidify components - embed live React previews with Apple HIG-inspired interactions and real-time code editing"
---

import { Preview } from '../components/Preview';

# Live Component Previews

Experience Liquidify components through interactive previews that showcase the liquid glass design language in action. Each preview demonstrates Apple HIG-inspired interactions with editable code examples.

## 🎯 Interactive Preview System

The Preview component provides a sophisticated sandbox for experimenting with Liquidify components, featuring real-time code editing, theme switching, and responsive testing.

### Core Preview Features

<Preview>
  <GlassButton 
    variant="primary"
    onClick={() => alert("Welcome to Liquidify!")}
    className="relative overflow-hidden group"
  >
    <span className="relative z-10">Experience Liquid Glass</span>
    <div className="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-purple-500/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
  </GlassButton>
</Preview>

```tsx
<GlassButton 
  variant="primary"
  onClick={() => alert("Welcome to Liquidify!")}
  className="relative overflow-hidden group"
>
  <span className="relative z-10">Experience Liquid Glass</span>
  <div className="absolute inset-0 bg-gradient-to-r from-blue-500/20 to-purple-500/20 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
</GlassButton>
```

## 🎨 Advanced Component Previews

### Magnetic Interaction Demonstration

<Preview>
  <div className="group relative inline-flex">
    <div className="absolute inset-0 rounded-xl blur-xl bg-blue-500/30 opacity-0 group-hover:opacity-100 transition-all duration-500 scale-110" />
    <GlassButton 
      variant="elevated"
      className="relative z-10 transform transition-transform duration-200 group-hover:scale-105"
    >
      Magnetic Hover Effect
    </GlassButton>
  </div>
</Preview>

```tsx
<div className="group relative inline-flex">
  <div className="absolute inset-0 rounded-xl blur-xl bg-blue-500/30 opacity-0 group-hover:opacity-100 transition-all duration-500 scale-110" />
  <GlassButton 
    variant="elevated"
    className="relative z-10 transform transition-transform duration-200 group-hover:scale-105"
  >
    Magnetic Hover Effect
  </GlassButton>
</div>
```

### Stateful Component Example

<Preview>
  <StateExample />
</Preview>

```tsx
function StateExample() {
  const [count, setCount] = React.useState(0);
  const [theme, setTheme] = React.useState('light');
  
  return (
    <div className="space-y-4 p-6 rounded-xl bg-liquid-surface/50 backdrop-blur-sm">
      <div className="flex items-center justify-between">
        <h3 className="font-semibold text-liquid-primary">Interactive Counter</h3>
        <GlassToggle 
          checked={theme === 'dark'}
          onCheckedChange={(checked) => setTheme(checked ? 'dark' : 'light')}
        />
      </div>
      
      <div className="text-center space-y-4">
        <div className="text-3xl font-bold text-liquid-accent">{count}</div>
        
        <div className="flex gap-2 justify-center">
          <GlassButton 
            variant="secondary" 
            size="sm"
            onClick={() => setCount(c => Math.max(0, c - 1))}
            disabled={count === 0}
          >
            Decrease
          </GlassButton>
          
          <GlassButton 
            variant="primary" 
            size="sm"
            onClick={() => setCount(c => c + 1)}
          >
            Increase
          </GlassButton>
        </div>
        
        <GlassButton 
          variant="ghost" 
          size="xs"
          onClick={() => setCount(0)}
          className="text-liquid-tertiary"
        >
          Reset
        </GlassButton>
      </div>
    </div>
  );
}
```

## 📝 Form Component Previews

### Real-time Validation Example

<Preview>
  <FormValidationExample />
</Preview>

```tsx
function FormValidationExample() {
  const [email, setEmail] = React.useState("");
  const [password, setPassword] = React.useState("");
  const [errors, setErrors] = React.useState({});
  
  const validateEmail = (value) => {
    return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value);
  };
  
  const validatePassword = (value) => {
    return value.length >= 8;
  };
  
  React.useEffect(() => {
    const newErrors = {};
    if (email && !validateEmail(email)) {
      newErrors.email = "Invalid email format";
    }
    if (password && !validatePassword(password)) {
      newErrors.password = "Password must be at least 8 characters";
    }
    setErrors(newErrors);
  }, [email, password]);
  
  return (
    <form className="space-y-4 max-w-sm">
      <GlassFormField 
        label="Email" 
        error={errors.email}
        success={email && !errors.email ? "Valid email" : undefined}
      >
        <GlassInput
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="your@email.com"
        />
      </GlassFormField>
      
      <GlassFormField 
        label="Password" 
        error={errors.password}
        success={password && !errors.password ? "Strong password" : undefined}
      >
        <GlassInput
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          placeholder="Enter password"
        />
      </GlassFormField>
      
      <GlassButton 
        type="submit" 
        className="w-full"
        disabled={Object.keys(errors).length > 0 || !email || !password}
      >
        Sign Up
      </GlassButton>
    </form>
  );
}
```

## 🎪 Layout & Navigation Previews

### Responsive Tab System

<Preview>
  <ResponsiveTabExample />
</Preview>

```tsx
function ResponsiveTabExample() {
  const [activeTab, setActiveTab] = React.useState("overview");
  
  const tabs = [
    { id: "overview", label: "Overview", icon: "🏠" },
    { id: "components", label: "Components", icon: "🧩" },
    { id: "patterns", label: "Patterns", icon: "🎨" },
    { id: "resources", label: "Resources", icon: "📚" }
  ];
  
  const content = {
    overview: "Welcome to Liquidify - a sophisticated React component library",
    components: "Explore our comprehensive collection of liquid glass components",
    patterns: "Discover advanced interaction patterns and design recipes",
    resources: "Documentation, guides, and additional learning materials"
  };
  
  return (
    <div className="w-full max-w-md">
      <GlassTabs value={activeTab} onValueChange={setActiveTab}>
        <GlassTabs.List className="grid grid-cols-2 lg:grid-cols-4 gap-1 p-1 bg-liquid-surface/30 rounded-lg">
          {tabs.map((tab) => (
            <GlassTabs.Trigger
              key={tab.id}
              value={tab.id}
              className="flex items-center gap-2 px-3 py-2 text-sm rounded-md transition-all"
            >
              <span>{tab.icon}</span>
              <span className="hidden sm:inline">{tab.label}</span>
            </GlassTabs.Trigger>
          ))}
        </GlassTabs.List>
        
        <div className="mt-6 p-4 rounded-lg bg-liquid-surface/20 backdrop-blur-sm">
          <p className="text-liquid-secondary text-sm">
            {content[activeTab]}
          </p>
        </div>
      </GlassTabs>
    </div>
  );
}
```

## 🎭 Modal & Overlay Previews

### Interactive Modal Demo

<Preview>
  <ModalDemo />
</Preview>

```tsx
function ModalDemo() {
  const [isOpen, setIsOpen] = React.useState(false);
  const [formData, setFormData] = React.useState({ name: "", email: "" });
  
  const handleSubmit = (e) => {
    e.preventDefault();
    console.log("Form submitted:", formData);
    setIsOpen(false);
    setFormData({ name: "", email: "" });
  };
  
  return (
    <>
      <GlassButton onClick={() => setIsOpen(true)}>
        Open Contact Form
      </GlassButton>
      
      <GlassModal open={isOpen} onOpenChange={setIsOpen}>
        <GlassModal.Content className="max-w-md">
          <GlassModal.Header>
            <GlassModal.Title>Get in Touch</GlassModal.Title>
            <GlassModal.Description>
              Send us a message and we'll get back to you.
            </GlassModal.Description>
          </GlassModal.Header>
          
          <form onSubmit={handleSubmit} className="space-y-4">
            <GlassFormField label="Name">
              <GlassInput
                value={formData.name}
                onChange={(e) => setFormData(prev => ({ ...prev, name: e.target.value }))}
                placeholder="Your name"
              />
            </GlassFormField>
            
            <GlassFormField label="Email">
              <GlassInput
                type="email"
                value={formData.email}
                onChange={(e) => setFormData(prev => ({ ...prev, email: e.target.value }))}
                placeholder="your@email.com"
              />
            </GlassFormField>
            
            <div className="flex gap-3 pt-4">
              <GlassButton type="submit" className="flex-1">
                Send Message
              </GlassButton>
              <GlassButton 
                type="button" 
                variant="secondary" 
                onClick={() => setIsOpen(false)}
              >
                Cancel
              </GlassButton>
            </div>
          </form>
        </GlassModal.Content>
      </GlassModal>
    </>
  );
}
```

## 🎨 Theme & Customization Previews

### Theme Switcher Demo

<Preview>
  <ThemeSwitcherDemo />
</Preview>

```tsx
function ThemeSwitcherDemo() {
  const [theme, setTheme] = React.useState("light");
  const [glassIntensity, setGlassIntensity] = React.useState(0.8);
  
  return (
    <div 
      className="p-6 rounded-xl transition-all duration-300"
      style={{
        backgroundColor: theme === 'dark' ? 'rgba(0,0,0,0.1)' : 'rgba(255,255,255,0.1)',
        backdropFilter: `blur(${glassIntensity * 20}px)`,
        border: '1px solid rgba(255,255,255,0.2)'
      }}
    >
      <div className="space-y-4">
        <h3 className="font-semibold">Theme Customization</h3>
        
        <div className="flex items-center gap-4">
          <label className="text-sm">Theme:</label>
          <GlassSegmentedControl
            value={theme}
            onValueChange={setTheme}
            options={[
              { value: "light", label: "Light" },
              { value: "dark", label: "Dark" }
            ]}
          />
        </div>
        
        <div className="space-y-2">
          <label className="text-sm block">Glass Intensity: {Math.round(glassIntensity * 100)}%</label>
          <GlassSlider
            value={[glassIntensity]}
            onValueChange={([value]) => setGlassIntensity(value)}
            min={0}
            max={1}
            step={0.1}
          />
        </div>
        
        <div className="pt-4 border-t border-liquid-border/50">
          <GlassButton variant="primary" className="mr-2">
            Primary Action
          </GlassButton>
          <GlassButton variant="secondary">
            Secondary Action
          </GlassButton>
        </div>
      </div>
    </div>
  );
}
```

## 📱 Responsive Design Previews

### Adaptive Card Layout

<Preview>
  <ResponsiveCardExample />
</Preview>

```tsx
function ResponsiveCardExample() {
  const [viewMode, setViewMode] = React.useState("grid");
  
  const cards = [
    { title: "Design System", description: "Comprehensive component library", icon: "🎨" },
    { title: "Performance", description: "60fps animations and optimization", icon: "⚡" },
    { title: "Accessibility", description: "WCAG 2.1 AA compliance", icon: "♿" }
  ];
  
  return (
    <div className="w-full">
      <div className="flex justify-between items-center mb-4">
        <h3 className="font-semibold">Featured Content</h3>
        <GlassToggleGroup
          value={viewMode}
          onValueChange={setViewMode}
          type="single"
        >
          <GlassToggleGroup.Item value="grid">Grid</GlassToggleGroup.Item>
          <GlassToggleGroup.Item value="list">List</GlassToggleGroup.Item>
        </GlassToggleGroup>
      </div>
      
      <div className={viewMode === "grid" ? "grid gap-4 sm:grid-cols-2 lg:grid-cols-3" : "space-y-3"}>
        {cards.map((card, index) => (
          <GlassCard 
            key={index} 
            variant="elevated"
            className={`p-4 ${viewMode === "list" ? "flex items-center gap-4" : ""}`}
          >
            <div className={`text-2xl ${viewMode === "list" ? "" : "mb-3"}`}>
              {card.icon}
            </div>
            <div className="flex-1">
              <h4 className="font-medium mb-1">{card.title}</h4>
              <p className="text-sm text-liquid-secondary">{card.description}</p>
            </div>
          </GlassCard>
        ))}
      </div>
    </div>
  );
}
```

---

## 🛠️ Implementation Guide

### Setting Up Live Previews

To implement this preview system in your documentation:

1. **Create the Preview Component**

```tsx
// apps/docs/components/Preview.tsx
import React from 'react';
import { GlassCard, GlassButton, GlassIcon } from '@liquidify/react';

interface PreviewProps {
  children: React.ReactNode;
  title?: string;
  description?: string;
}

export function Preview({ children, title, description }: PreviewProps) {
  const [copied, setCopied] = React.useState(false);
  
  const copyCode = async () => {
    // Implementation for copying code
    setCopied(true);
    setTimeout(() => setCopied(false), 2000);
  };
  
  return (
    <GlassCard variant="elevated" className="mb-6">
      {(title || description) && (
        <div className="p-4 border-b border-liquid-border/50">
          {title && <h3 className="font-semibold mb-1">{title}</h3>}
          {description && <p className="text-sm text-liquid-secondary">{description}</p>}
        </div>
      )}
      
      <div className="p-6 bg-gradient-to-br from-liquid-surface/30 to-liquid-surface/10">
        {children}
      </div>
      
      <div className="px-4 py-2 bg-liquid-surface/20 border-t border-liquid-border/50">
        <GlassButton
          variant="ghost"
          size="sm"
          onClick={copyCode}
          className="ml-auto"
        >
          <GlassIcon name={copied ? "check" : "clipboard"} className="mr-2" />
          {copied ? "Copied!" : "Copy Code"}
        </GlassButton>
      </div>
    </GlassCard>
  );
}
```

2. **Configure MDX Integration**

```typescript
// apps/docs/next.config.js
const withMDX = require('@next/mdx')({
  extension: /\.mdx?$/,
  options: {
    remarkPlugins: [],
    rehypePlugins: [],
    providerImportSource: "@mdx-js/react"
  }
});

module.exports = withMDX({
  pageExtensions: ['ts', 'tsx', 'js', 'jsx', 'md', 'mdx'],
});
```

## ♿ Accessibility Considerations

<Card>
  <Card.Content>
    **Keyboard Navigation**: All interactive previews support full keyboard navigation with proper focus management.
    
    **Screen Reader Support**: Preview components include appropriate ARIA labels and descriptions for assistive technologies.
    
    **Reduced Motion**: Animations and transitions respect `prefers-reduced-motion` user preferences.
    
    **Color Contrast**: All preview content maintains WCAG 2.1 AA color contrast ratios across themes.
  </Card.Content>
</Card>

## 📚 Related Resources

- [Component Storybook](https://storybook.liquidify.dev) - Comprehensive component documentation
- [Apple HIG Integration](/guides/apple-hig-integration) - Platform-specific design patterns
- [Theming System](/guides/theming) - Advanced customization techniques
- [Accessibility Guide](/guides/accessibility) - Inclusive design standards
