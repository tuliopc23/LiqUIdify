---
title: "Typography System"
description: "Explore LiqUIdify's comprehensive typography system built on Apple's Human Interface Guidelines"
---
# Typography System

LiqUIdify's typography system combines **Apple's Human Interface Guidelines** with modern web typography best practices to create beautiful, readable, and accessible text across all devices.

## 🎯 Design Principles

<CardGroup cols={2}>
  <Card title="🍎 Apple Quality" icon="apple">
    SF Pro font family with precise letter spacing and authentic Apple
    typography scales
  </Card>
  <Card title="📖 Readability First" icon="book-open">
    Optimized line heights, contrast ratios, and reading widths for maximum
    comprehension
  </Card>
  <Card title="♿ Accessibility Built-in" icon="universal-access">
    WCAG 2.1 AA compliant with proper contrast, focus indicators, and screen
    reader support
  </Card>
  <Card title="📱 Device Optimized" icon="mobile">
    Responsive typography that adapts beautifully from mobile to desktop
  </Card>
</CardGroup>

## 📝 Font Families

### Display Fonts

```css
font-family:
  "SF Pro Display",
  "Inter",
  -apple-system,
  system-ui;
```

Used for **headlines, titles, and large text** where impact and visual hierarchy are important.

### Text Fonts

```css
font-family:
  "SF Pro Text",
  "Inter",
  -apple-system,
  system-ui;
```

Optimized for **body text, paragraphs, and UI elements** with superior readability at smaller sizes.

### Monospace Fonts

```css
font-family: "SF Mono", "Fira Code", "JetBrains Mono", monospace;
```

Perfect for **code blocks, technical content,** and data that requires fixed-width alignment.
---

## 📏 Typography Scale

Our typography scale is based on a **modular scale** that ensures consistent visual hierarchy:

| Class        | Size | Use Case              | Example                                                            |
| ------------ | ---- | --------------------- | ------------------------------------------------------------------ |
| `.text-xs`   | 12px | Captions, metadata    | <span className="text-xs">Small supporting text</span>             |
| `.text-sm`   | 14px | Labels, small UI text | <span className="text-sm">Navigation items</span>                  |
| `.text-base` | 16px | Body text             | <span className="text-base">This is standard paragraph text</span> |
| `.text-lg`   | 18px | Lead paragraphs       | <span className="text-lg">Important introductory text</span>       |
| `.text-xl`   | 20px | Small headings        | <span className="text-xl font-semibold">Section Headers</span>     |
| `.text-2xl`  | 24px | Card titles           | <span className="text-2xl font-semibold">Component Titles</span>   |
| `.text-3xl`  | 30px | Page sections         | <span className="text-3xl font-bold">Major Sections</span>         |
| `.text-4xl`  | 36px | Page titles           | <span className="text-4xl font-bold">Page Headlines</span>         |
| `.text-5xl`  | 48px | Hero headings         | <span className="text-5xl font-bold">Hero Titles</span>            |

---

## 🎨 Text Colors

Our color system ensures proper contrast ratios for accessibility:

<CardGroup cols={2}>
  <Card title="Light Theme">
    - **Primary**: `#111827` (AAA contrast) - **Secondary**: `#6B7280` (AA
    contrast) - **Tertiary**: `#9CA3AF` (AA contrast) - **Accent**: `#007AFF`
    (Apple blue)
  </Card>
  <Card title="Dark Theme">
    - **Primary**: `#F9FAFB` (AAA contrast) - **Secondary**: `#D1D5DB` (AA
    contrast) - **Tertiary**: `#9CA3AF` (AA contrast) - **Accent**: `#3B82F6`
    (Optimized blue)
  </Card>
</CardGroup>

### Color Examples

<div className="space-y-4">
  <p className="text-primary">This is primary text - used for main content</p>
  <p className="text-secondary">
    This is secondary text - used for descriptions
  </p>
  <p className="text-tertiary">This is tertiary text - used for metadata</p>
  <p className="text-accent">
    This is accent text - used for links and highlights
  </p>
  <p className="text-success">
    This is success text - used for positive states
  </p>
  <p className="text-warning">This is warning text - used for caution states</p>
  <p className="text-error">This is error text - used for error states</p>
</div>

---

## ⚖️ Font Weights

Carefully selected weights that work harmoniously together:

<div className="space-y-2">
  <p className="font-light">Light (300) - Elegant, minimal emphasis</p>
  <p className="font-normal">Normal (400) - Standard body text</p>
  <p className="font-medium">Medium (500) - Subtle emphasis, UI elements</p>
  <p className="font-semibold">Semibold (600) - Headings, important text</p>
  <p className="font-bold">Bold (700) - Strong emphasis, titles</p>
  <p className="font-extrabold">Extra Bold (800) - Hero headlines</p>
</div>

---

## 📐 Line Heights

Optimized for different content types:

```css
--leading-tight: 1.25; /* Large headings */
--leading-snug: 1.375; /* Medium headings */
--leading-normal: 1.5; /* UI text */
--leading-relaxed: 1.625; /* Body paragraphs */
--leading-loose: 2; /* Spaced content */
```

### Line Height Examples

<div className="space-y-4">
  <h3 className="leading-tight">
    This heading uses tight line height (1.25) for better visual impact and
    space efficiency
  </h3>
  <p className="leading-normal">
    This paragraph uses normal line height (1.5) which is perfect for user
    interface text and shorter content blocks.
  </p>
  <p className="leading-relaxed">
    This paragraph uses relaxed line height (1.625) which provides superior
    readability for longer form content like documentation, articles, and
    detailed explanations where reading comfort is paramount.
  </p>
</div>

---

## 🔤 Letter Spacing

Subtle adjustments that improve readability:

```css
--tracking-tighter: -0.05em; /* Large headings */
--tracking-tight: -0.025em; /* Medium headings */
--tracking-normal: 0em; /* Body text */
--tracking-wide: 0.025em; /* Small caps, labels */
--tracking-wider: 0.05em; /* Buttons, badges */
```

### Letter Spacing Examples

<div className="space-y-2">
  <h2 className="tracking-tighter">Hero Headline with Tighter Tracking</h2>
  <h3 className="tracking-tight">Section Header with Tight Tracking</h3>
  <p className="tracking-normal">Normal body text with standard tracking</p>
  <p className="text-sm tracking-wide uppercase">
    Small caps with wide tracking
  </p>
</div>

---

## 💻 Code Typography

Optimized for technical content with proper syntax highlighting:

### Inline Code

Use `inline code` for short snippets, variable names, and technical terms.

```css
/* Inline code styling */
code {
  font-family: var(--font-mono);
  font-size: 0.875em;
  background: rgba(59, 130, 246, 0.1);
  color: var(--text-accent);
  padding: 0.125rem 0.375rem;
  border-radius: 0.375rem;
}
```

### Code Blocks

```typescript
// Multi-line code with syntax highlighting
;

interface ComponentProps {
  title: string;
  variant?: "primary" | "secondary";
  children: React.ReactNode;
}

export function MyComponent({ title, variant = "primary", children }: ComponentProps) {
  return (
    <Card className="liquid-glass">
      <h2 className="text-2xl font-semibold text-primary">{title}</h2>
      <div className="mt-4">{children}</div>
      <Button variant={variant}>Get Started</Button>
    </Card>
  );
}
```

---

## 📋 Lists & Content

### Unordered Lists

- **Primary benefits** of the typography system
- **Consistent visual hierarchy** across all components
- **Improved readability** with optimized line heights
- **Accessibility compliance** with proper contrast ratios

### Ordered Lists

1. **Choose appropriate font family** for your content type
2. **Select proper size** from the typography scale
3. **Apply correct line height** for readability
4. **Ensure sufficient color contrast** for accessibility

### Definition Lists

**Typography Scale**
: A harmonious progression of font sizes that creates visual hierarchy

**Line Height**
: The vertical space between lines of text, crucial for readability

**Letter Spacing**
: Horizontal space between characters, fine-tuned for optimal reading

---

## 📊 Data Tables

Our typography system includes optimized table styles:

| Component     | Font Size | Weight   | Line Height | Use Case          |
| ------------- | --------- | -------- | ----------- | ----------------- |
| Table Header  | 14px      | Semibold | 1.25        | Column titles     |
| Table Cell    | 14px      | Normal   | 1.5         | Data content      |
| Table Caption | 12px      | Medium   | 1.25        | Table description |

---

## 💬 Blockquotes

> Our typography system is designed to be both beautiful and functional. Every decision is made with readability, accessibility, and user experience in mind.
>
> — LiqUIdify Design Team

---

## 🎯 Best Practices

<CardGroup cols={2}>
  <Card title="✅ Do" icon="check">
    - Use semantic HTML elements (`h1`, `p`, `strong`) - Maintain consistent
    line heights within sections - Ensure 4.5:1 contrast ratio minimum - Test
    readability on different devices - Use appropriate font weights for emphasis
  </Card>
  <Card title="❌ Don't" icon="x">
    - Mix different font families randomly - Use too many font weights in one
    design - Ignore mobile readability - Create insufficient color contrast -
    Override system fonts without good reason
  </Card>
</CardGroup>

---

## 🔧 Implementation

### CSS Custom Properties

```css
/* Use our typography system */
.my-component {
  font-family: var(--font-text);
  font-size: var(--text-base);
  line-height: var(--leading-relaxed);
  color: var(--text-primary);
}

/* Responsive heading */
.my-heading {
  font-family: var(--font-display);
  font-size: var(--text-3xl);
  font-weight: var(--font-semibold);
  line-height: var(--leading-tight);
  letter-spacing: var(--tracking-tight);
}
```

### Utility Classes

```html

<h1
  class="font-display text-5xl font-bold leading-tight tracking-tighter text-primary"
>
  Hero Headline
</h1>

<p class="font-text text-lg leading-relaxed text-secondary">
  Lead paragraph with enhanced readability
</p>

<code class="font-mono text-sm text-accent">
  const example = "inline code";
</code>
```

---

## 📱 Responsive Behavior

Our typography automatically adapts to different screen sizes:

- **Mobile (≤640px)**: Smaller base font size (14px), reduced heading sizes
- **Tablet (641px-1024px)**: Medium base font size (15px), balanced proportions
- **Desktop (≥1025px)**: Full base font size (16px), optimal reading experience

### Mobile Optimizations

```css
@media (max-width: 640px) {
  html {
    font-size: 14px;
  }

  h1 {
    font-size: var(--text-4xl);
  }
  h2 {
    font-size: var(--text-3xl);
  }

  .prose {
    padding: 0 1rem;
    max-width: 100%;
  }
}
```

---

<Note>
  **Performance Note**: Our typography system uses `font-display: swap` for
  better loading performance and includes comprehensive fallback fonts to ensure
  text remains readable even if custom fonts fail to load.
</Note>

Ready to implement beautiful typography in your project? Check out our [Component Examples](/components) to see the typography system in action!
