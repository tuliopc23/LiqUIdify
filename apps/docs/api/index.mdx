---
title: API Reference
description: Complete public API of the liquidify package with usage notes.
---

# Public API

This page summarizes the exports from `liquidify` and points to common props and patterns.

## Providers

- `GlassUIProvider` — Wraps your app and orchestrates Theme, Liquid Glass, Haptics, Live Region and Toast.

```tsx
import { GlassUIProvider } from "liquidify";
import "liquidify/styles";

export default function App({ children }) {
  return (
    <GlassUIProvider theme="light" glassConfig={{ blur: 4, saturation: 120 }}>
      {children}
    </GlassUIProvider>
  );
}
```

Props:

- `theme`: "light" | "dark" (default: "light")
- `glassConfig`: { intensity?: number; blur?: number; saturation?: number; enableMagnetic?: boolean; enableSpecular?: boolean }
- `hapticConfig`: { enableVibration?: boolean; enableAudio?: boolean; enableVisual?: boolean }

## Hooks

- `useTheme()` — Returns `{ theme, setTheme }`.
- `useLiquidGlass()` — Returns liquid-glass runtime configuration and helpers.
- `useHapticFeedback()` — Trigger subtle feedback on interactions.
- `useSsrSafeHooks()` / `useSsrSafe()` — Guards for SSR.
- `usePerformanceMonitoring()` — Perf insight utilities for components.

Example (live):

```tsx live
import { useTheme, GlassButton } from "liquidify";

export default function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  return (
    <GlassButton
      variant="ghost"
      onClick={() => setTheme(theme === "light" ? "dark" : "light")}
    >
      Toggle: {theme}
    </GlassButton>
  );
}
```

## Components (selected)

- Buttons: `GlassButton`, `GlassResponsiveButton`
- Cards: `GlassCard` (refactored), `GlassResponsiveCard`
- Inputs: `GlassInput`, `GlassCheckbox`, `GlassRadioGroup`, `GlassSelect`, `GlassCombobox`, `GlassNumberInput`, `GlassTextarea`, `GlassFormField`
- Feedback: `GlassToast`, `GlassSpinner`, `GlassProgress`, `GlassSkeleton`, `GlassAlert`, `GlassNotification`
- Layout/Navigation: `GlassModal`, `GlassDrawer`, `GlassDropdown`, `Navbar`, `Sidebar`, `GlassBreadcrumbs`, `GlassMobileNav`
- Data: `GlassTable`, `GlassTabs`, `GlassTimeline`, `GlassTooltip`, `GlassPopover`, `GlassTreeView`, `GlassChart`, `GlassPagination`
- Accessibility: `GlassErrorBoundary`, `GlassFocusTrap`, `GlassSkipNavigation`, `GlassLiveRegion`

Import and usage are consistent:

```tsx
import { GlassButton } from "liquidify";

<GlassButton variant="primary" size="lg">
  Label
</GlassButton>;
```

For any component props, check your editor’s TypeScript hints — types ship with the package.

## Full Exports

The package currently exports the following production modules:

- glass-accordion
- glass-alert
- glass-avatar
- glass-badge
- glass-banner
- glass-breadcrumbs
- glass-button-refactored
- glass-card-refactored
- glass-chart
- glass-checkbox
- glass-checkbox-group
- glass-combobox
- glass-command
- glass-date-picker
- glass-drawer
- glass-dropdown
- glass-error-boundary
- glass-file-upload
- glass-focus-trap
- glass-form-field
- glass-input
- glass-loading
- glass-mobile-nav
- glass-modal
- glass-notification
- glass-number-input
- glass-pagination
- glass-popover
- glass-portal
- glass-progress
- glass-radio-group
- glass-responsive-button
- glass-responsive-card
- glass-search
- glass-select
- glass-skeleton
- glass-skip-navigation
- glass-slider
- glass-spinner
- glass-switch
- glass-table
- glass-tabs
- glass-textarea
- glass-timeline
- glass-toast
- glass-tooltip
- glass-tree-view
- navbar
- sidebar
- theme-provider
- theme-toggle
