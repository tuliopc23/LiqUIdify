---
title: "Glass Card"
description: "Flexible container component with beautiful glassmorphism effects and compound component pattern"
---

# Glass Card

The `GlassCard` component is a versatile container that provides the perfect foundation for building elegant interfaces with glassmorphism effects. It supports compound components for structured content layouts.

## Import

<CodeGroup>

```tsx Component Import
import { GlassCard } from '@liquidify/components'
```

```tsx Bundle Import  
import { GlassCard } from '@liquidify/components/core'
```

```tsx Compound Components
import { 
  GlassCard,
  GlassCardHeader,
  GlassCardTitle,
  GlassCardDescription,
  GlassCardContent,
  GlassCardFooter 
} from '@liquidify/components'
```

</CodeGroup>

## Basic Usage

<CodeGroup>

```tsx Simple Card
<GlassCard className="p-6">
  <h3 className="text-lg font-semibold mb-2">Card Title</h3>
  <p className="text-gray-600">
    This is a simple glass card with basic content.
  </p>
</GlassCard>
```

```tsx With Variants
<div className="grid gap-4 md:grid-cols-2">
  <GlassCard variant="default" className="p-6">
    <h3>Default Card</h3>
    <p>Standard glass effect</p>
  </GlassCard>
  
  <GlassCard variant="elevated" className="p-6">
    <h3>Elevated Card</h3>
    <p>Enhanced elevation and depth</p>
  </GlassCard>
</div>
```

</CodeGroup>

## Interactive Preview

<ComponentPreview>
  <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
    <GlassCard variant="default" className="p-6">
      <h3 className="font-semibold mb-2">Default</h3>
      <p className="text-sm text-gray-600">Standard glass effect with subtle transparency</p>
    </GlassCard>
    
    <GlassCard variant="elevated" className="p-6">
      <h3 className="font-semibold mb-2">Elevated</h3>
      <p className="text-sm text-gray-600">Enhanced depth with stronger shadows</p>
    </GlassCard>
    
    <GlassCard variant="outline" className="p-6">
      <h3 className="font-semibold mb-2">Outline</h3>
      <p className="text-sm text-gray-600">Minimal style with border emphasis</p>
    </GlassCard>
  </div>
</ComponentPreview>

## Variants

### Default
Standard glass effect for general use:

<ComponentPreview>
  <GlassCard variant="default" className="p-6 max-w-sm">
    <h3 className="text-lg font-semibold mb-2">Default Card</h3>
    <p className="text-gray-600">
      Perfect balance of glass effect and readability for everyday use.
    </p>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard variant="default" className="p-6">
  <h3>Default Card</h3>
  <p>Content goes here</p>
</GlassCard>
```

### Elevated
Enhanced glass effect with greater depth:

<ComponentPreview>
  <GlassCard variant="elevated" className="p-6 max-w-sm">
    <h3 className="text-lg font-semibold mb-2">Elevated Card</h3>
    <p className="text-gray-600">
      Stronger glass effects for important content that needs to stand out.
    </p>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard variant="elevated" className="p-6">
  <h3>Elevated Card</h3>
  <p>Important content</p>
</GlassCard>
```

### Outline
Minimal glass with border emphasis:

<ComponentPreview>
  <GlassCard variant="outline" className="p-6 max-w-sm">
    <h3 className="text-lg font-semibold mb-2">Outline Card</h3>
    <p className="text-gray-600">
      Clean and minimal design with subtle border definition.
    </p>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard variant="outline" className="p-6">
  <h3>Outline Card</h3>
  <p>Minimal content</p>
</GlassCard>
```

### Ghost
Ultra-subtle glass effect:

<ComponentPreview>
  <GlassCard variant="ghost" className="p-6 max-w-sm">
    <h3 className="text-lg font-semibold mb-2">Ghost Card</h3>
    <p className="text-gray-600">
      Nearly invisible glass effect for subtle content separation.
    </p>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard variant="ghost" className="p-6">
  <h3>Ghost Card</h3>
  <p>Subtle content</p>
</GlassCard>
```

## Compound Components

Use compound components for structured content layouts:

<ComponentPreview>
  <GlassCard variant="elevated" className="max-w-md">
    <GlassCardHeader>
      <GlassCardTitle>Project Dashboard</GlassCardTitle>
      <GlassCardDescription>
        Monitor your project's progress and key metrics
      </GlassCardDescription>
    </GlassCardHeader>
    
    <GlassCardContent>
      <div className="space-y-4">
        <div className="flex justify-between">
          <span>Tasks Completed</span>
          <span className="font-semibold">24/30</span>
        </div>
        <div className="flex justify-between">
          <span>Team Members</span>
          <span className="font-semibold">8</span>
        </div>
        <div className="flex justify-between">
          <span>Deadline</span>
          <span className="font-semibold">Dec 15, 2024</span>
        </div>
      </div>
    </GlassCardContent>
    
    <GlassCardFooter>
      <GlassButton variant="primary" fullWidth>
        View Details
      </GlassButton>
    </GlassCardFooter>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard variant="elevated">
  <GlassCardHeader>
    <GlassCardTitle>Project Dashboard</GlassCardTitle>
    <GlassCardDescription>
      Monitor your project's progress and key metrics
    </GlassCardDescription>
  </GlassCardHeader>
  
  <GlassCardContent>
    <div className="space-y-4">
      <div className="flex justify-between">
        <span>Tasks Completed</span>
        <span className="font-semibold">24/30</span>
      </div>
      {/* More content */}
    </div>
  </GlassCardContent>
  
  <GlassCardFooter>
    <GlassButton variant="primary" fullWidth>
      View Details
    </GlassButton>
  </GlassCardFooter>
</GlassCard>
```

## Interactive Features

### Hover Effects

<ComponentPreview>
  <GlassCard 
    variant="default" 
    hover 
    className="p-6 max-w-sm cursor-pointer transition-transform hover:scale-105"
  >
    <h3 className="text-lg font-semibold mb-2">Hover Card</h3>
    <p className="text-gray-600">
      Hover over this card to see the interactive effect.
    </p>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard 
  variant="default" 
  hover
  className="p-6 cursor-pointer"
>
  <h3>Interactive Card</h3>
  <p>Hover to see effect</p>
</GlassCard>
```

### Selectable Cards

<ComponentPreview>
  <div className="grid gap-4 md:grid-cols-2">
    <GlassCard 
      selectable 
      selected={false}
      variant="outline"
      className="p-6 cursor-pointer"
      onClick={() => {}}
    >
      <h3 className="font-semibold mb-2">Option A</h3>
      <p className="text-sm text-gray-600">Click to select this option</p>
    </GlassCard>
    
    <GlassCard 
      selectable 
      selected={true}
      variant="outline"
      className="p-6 cursor-pointer"
      onClick={() => {}}
    >
      <h3 className="font-semibold mb-2">Option B</h3>
      <p className="text-sm text-gray-600">This option is selected</p>
    </GlassCard>
  </div>
</ComponentPreview>

```tsx
<GlassCard 
  selectable 
  selected={isSelected}
  onCardSelect={setIsSelected}
  variant="outline"
>
  <h3>Selectable Option</h3>
  <p>Click to toggle selection</p>
</GlassCard>
```

### Clickable Cards

<ComponentPreview>
  <GlassCard 
    interactive
    className="p-6 max-w-sm cursor-pointer"
    onCardClick={() => alert('Card clicked!')}
  >
    <h3 className="text-lg font-semibold mb-2">Clickable Card</h3>
    <p className="text-gray-600">
      This entire card is clickable. Try clicking anywhere!
    </p>
    <div className="mt-4 text-blue-600 text-sm font-medium">
      Click me →
    </div>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard 
  interactive
  onCardClick={() => handleCardClick()}
>
  <h3>Clickable Card</h3>
  <p>Entire card responds to clicks</p>
</GlassCard>
```

## Advanced Layouts

### Card Grid

<ComponentPreview>
  <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    <GlassCard variant="elevated">
      <GlassCardHeader>
        <GlassCardTitle>Analytics</GlassCardTitle>
        <GlassCardDescription>
          View your site analytics
        </GlassCardDescription>
      </GlassCardHeader>
      <GlassCardContent>
        <div className="text-2xl font-bold text-blue-600">
          24,590
        </div>
        <p className="text-sm text-gray-600">Page views this month</p>
      </GlassCardContent>
    </GlassCard>
    
    <GlassCard variant="elevated">
      <GlassCardHeader>
        <GlassCardTitle>Revenue</GlassCardTitle>
        <GlassCardDescription>
          Monthly revenue tracking
        </GlassCardDescription>
      </GlassCardHeader>
      <GlassCardContent>
        <div className="text-2xl font-bold text-green-600">
          $12,450
        </div>
        <p className="text-sm text-gray-600">Revenue this month</p>
      </GlassCardContent>
    </GlassCard>
    
    <GlassCard variant="elevated">
      <GlassCardHeader>
        <GlassCardTitle>Users</GlassCardTitle>
        <GlassCardDescription>
          Active user count
        </GlassCardDescription>
      </GlassCardHeader>
      <GlassCardContent>
        <div className="text-2xl font-bold text-purple-600">
          1,247
        </div>
        <p className="text-sm text-gray-600">Active users today</p>
      </GlassCardContent>
    </GlassCard>
  </div>
</ComponentPreview>

### Horizontal Layout

<ComponentPreview>
  <GlassCard variant="default" orientation="horizontal" className="max-w-2xl">
    <div className="flex">
      <div className="w-24 h-24 bg-gradient-to-br from-blue-400 to-purple-500 rounded-lg flex-shrink-0"></div>
      <div className="ml-6 flex-1">
        <GlassCardHeader className="pb-2">
          <GlassCardTitle>Horizontal Card</GlassCardTitle>
          <GlassCardDescription>
            Perfect for list items and media content
          </GlassCardDescription>
        </GlassCardHeader>
        <GlassCardContent>
          <p className="text-sm text-gray-600">
            This layout works great for product listings, user profiles, 
            or any content that benefits from a horizontal arrangement.
          </p>
        </GlassCardContent>
      </div>
    </div>
  </GlassCard>
</ComponentPreview>

```tsx
<GlassCard orientation="horizontal" className="max-w-2xl">
  <div className="flex">
    <div className="w-24 h-24 bg-gradient-to-br from-blue-400 to-purple-500 rounded-lg flex-shrink-0"></div>
    <div className="ml-6 flex-1">
      <GlassCardHeader>
        <GlassCardTitle>Horizontal Card</GlassCardTitle>
        <GlassCardDescription>
          Perfect for list items and media content
        </GlassCardDescription>
      </GlassCardHeader>
      <GlassCardContent>
        <p>Content in horizontal layout</p>
      </GlassCardContent>
    </div>
  </div>
</GlassCard>
```

## Real-World Examples

### User Profile Card

<ComponentPreview>
  <GlassCard variant="elevated" className="max-w-sm">
    <GlassCardHeader className="text-center">
      <div className="w-20 h-20 bg-gradient-to-br from-blue-500 to-purple-600 rounded-full mx-auto mb-4 flex items-center justify-center text-white font-semibold text-xl">
        JD
      </div>
      <GlassCardTitle>John Doe</GlassCardTitle>
      <GlassCardDescription>
        Senior Frontend Developer
      </GlassCardDescription>
    </GlassCardHeader>
    
    <GlassCardContent>
      <div className="space-y-3">
        <div className="flex items-center text-sm">
          <MapPin className="h-4 w-4 mr-2 text-gray-500" />
          <span>San Francisco, CA</span>
        </div>
        <div className="flex items-center text-sm">
          <Mail className="h-4 w-4 mr-2 text-gray-500" />
          <span>john@example.com</span>
        </div>
        <div className="flex items-center text-sm">
          <Calendar className="h-4 w-4 mr-2 text-gray-500" />
          <span>Joined March 2023</span>
        </div>
      </div>
    </GlassCardContent>
    
    <GlassCardFooter>
      <GlassButton variant="primary" fullWidth>
        View Profile
      </GlassButton>
    </GlassCardFooter>
  </GlassCard>
</ComponentPreview>

### Pricing Card

<ComponentPreview>
  <GlassCard variant="elevated" className="max-w-sm">
    <GlassCardHeader className="text-center">
      <GlassCardTitle className="text-2xl">Pro Plan</GlassCardTitle>
      <GlassCardDescription>
        Perfect for growing teams
      </GlassCardDescription>
      <div className="mt-4">
        <span className="text-3xl font-bold">$29</span>
        <span className="text-gray-600">/month</span>
      </div>
    </GlassCardHeader>
    
    <GlassCardContent>
      <ul className="space-y-3">
        <li className="flex items-center">
          <Check className="h-4 w-4 text-green-500 mr-3" />
          <span className="text-sm">Up to 50 team members</span>
        </li>
        <li className="flex items-center">
          <Check className="h-4 w-4 text-green-500 mr-3" />
          <span className="text-sm">Advanced analytics</span>
        </li>
        <li className="flex items-center">
          <Check className="h-4 w-4 text-green-500 mr-3" />
          <span className="text-sm">Priority support</span>
        </li>
        <li className="flex items-center">
          <Check className="h-4 w-4 text-green-500 mr-3" />
          <span className="text-sm">Custom integrations</span>
        </li>
      </ul>
    </GlassCardContent>
    
    <GlassCardFooter>
      <GlassButton variant="primary" fullWidth>
        Choose Plan
      </GlassButton>
    </GlassCardFooter>
  </GlassCard>
</ComponentPreview>

### Product Card

<ComponentPreview>
  <GlassCard variant="default" className="max-w-sm">
    <div className="h-48 bg-gradient-to-br from-pink-400 to-orange-300 rounded-t-lg"></div>
    
    <GlassCardHeader>
      <GlassCardTitle>Wireless Headphones</GlassCardTitle>
      <GlassCardDescription>
        Premium noise-cancelling headphones
      </GlassCardDescription>
    </GlassCardHeader>
    
    <GlassCardContent>
      <div className="flex items-center justify-between">
        <div>
          <span className="text-2xl font-bold text-green-600">$199</span>
          <span className="text-sm text-gray-500 line-through ml-2">$249</span>
        </div>
        <div className="flex">
          {[1,2,3,4,5].map((star) => (
            <Star key={star} className="h-4 w-4 fill-yellow-400 text-yellow-400" />
          ))}
        </div>
      </div>
      
      <p className="text-sm text-gray-600 mt-3">
        Experience crystal-clear audio with advanced noise cancellation technology.
      </p>
    </GlassCardContent>
    
    <GlassCardFooter className="space-y-2">
      <GlassButton variant="primary" fullWidth>
        Add to Cart
      </GlassButton>
      <GlassButton variant="ghost" fullWidth>
        View Details
      </GlassButton>
    </GlassCardFooter>
  </GlassCard>
</ComponentPreview>

## Props

<ResponseField name="variant" type="string" default="default">
  The visual style of the card
  - `"default"` - Standard glass effect
  - `"elevated"` - Enhanced depth and shadows
  - `"outline"` - Minimal style with border emphasis
  - `"ghost"` - Ultra-subtle glass effect
</ResponseField>

<ResponseField name="orientation" type="string" default="vertical">
  The layout orientation
  - `"vertical"` - Traditional card layout
  - `"horizontal"` - Side-by-side layout
</ResponseField>

<ResponseField name="hover" type="boolean" default="false">
  Enables enhanced hover effects
</ResponseField>

<ResponseField name="interactive" type="boolean" default="false">
  Makes the entire card clickable
</ResponseField>

<ResponseField name="selectable" type="boolean" default="false">
  Enables selection state styling
</ResponseField>

<ResponseField name="selected" type="boolean" default="false">
  Controls selection state (requires `selectable`)
</ResponseField>

<ResponseField name="bordered" type="boolean" default="false">
  Adds visible border to the card
</ResponseField>

<ResponseField name="elevation" type="string" default="none">
  Controls shadow depth
  - `"none"` - No shadow
  - `"sm"` - Small shadow
  - `"md"` - Medium shadow
  - `"lg"` - Large shadow
  - `"xl"` - Extra large shadow
</ResponseField>

<ResponseField name="glassEffect" type="object">
  Custom glass effect configuration
  ```tsx
  {
    intensity?: 'light' | 'medium' | 'strong' | 'ultra'
    blur?: boolean
    backdrop?: boolean
  }
  ```
</ResponseField>

<ResponseField name="asChild" type="boolean" default="false">
  When true, the component will render as its child element
</ResponseField>

## Events

<ResponseField name="onCardClick" type="(event: MouseEvent) => void">
  Called when the card is clicked (requires `interactive`)
</ResponseField>

<ResponseField name="onCardSelect" type="(selected: boolean) => void">
  Called when selection state changes (requires `selectable`)
</ResponseField>

<ResponseField name="onCardHover" type="(hovered: boolean) => void">
  Called when hover state changes
</ResponseField>

## Compound Component Props

### GlassCardHeader

<ResponseField name="className" type="string">
  Additional CSS classes
</ResponseField>

### GlassCardTitle

<ResponseField name="asChild" type="boolean" default="false">
  Render as child element
</ResponseField>

<ResponseField name="className" type="string">
  Additional CSS classes
</ResponseField>

### GlassCardDescription

<ResponseField name="className" type="string">
  Additional CSS classes
</ResponseField>

### GlassCardContent

<ResponseField name="className" type="string">
  Additional CSS classes
</ResponseField>

### GlassCardFooter

<ResponseField name="className" type="string">
  Additional CSS classes
</ResponseField>

## Accessibility

LiqUIdify cards follow accessibility best practices:

- **Semantic Structure**: Uses appropriate HTML elements and ARIA roles
- **Keyboard Navigation**: Interactive cards support Tab and Enter keys
- **Screen Reader Support**: Proper labeling and state announcements
- **Focus Management**: Clear focus indicators for interactive states
- **Selection States**: Properly communicated via `aria-selected`

### Accessibility Example

```tsx
<GlassCard
  interactive
  selectable
  selected={isSelected}
  onCardClick={handleCardClick}
  onCardSelect={setIsSelected}
  aria-label="Product card for Wireless Headphones"
  aria-describedby="card-description"
  role="button"
  tabIndex={0}
>
  <GlassCardHeader>
    <GlassCardTitle id="card-title">
      Wireless Headphones  
    </GlassCardTitle>
    <GlassCardDescription id="card-description">
      Premium noise-cancelling headphones with 30-hour battery life
    </GlassCardDescription>
  </GlassCardHeader>
  
  <GlassCardContent>
    {/* Card content */}
  </GlassCardContent>
</GlassCard>
```

## Theming

Customize card appearance using CSS variables:

```css
.custom-glass-card {
  --glass-card-bg: rgba(255, 255, 255, 0.1);
  --glass-card-border: rgba(255, 255, 255, 0.2);
  --glass-card-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
  --glass-card-radius: 16px;
  
  /* Selected state */
  --glass-card-selected-bg: rgba(59, 130, 246, 0.1);
  --glass-card-selected-border: rgba(59, 130, 246, 0.3);
  
  /* Hover state */
  --glass-card-hover-bg: rgba(255, 255, 255, 0.15);
  --glass-card-hover-border: rgba(255, 255, 255, 0.3);
}
```

## Performance Notes

- Cards are optimized with `React.memo` for efficient re-renders
- Glass effects are GPU-accelerated using CSS transforms
- Intersection Observer used for scroll-based animations
- Event handlers are bound efficiently to minimize memory usage
- Large lists should use virtualization for optimal performance

## Related Components

- [Glass Button](/components/glass-button) - Interactive buttons with glass effects
- [Glass Modal](/components/glass-modal) - Modal dialogs with glassmorphism
- [Glass Container](/components/glass-container) - Layout containers with glass effects