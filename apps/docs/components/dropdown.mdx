---
title: Dropdown
description: Menu-style selection anchored to a trigger.
---

import Preview from "./Preview";
import * as React from "react";

<Preview
  title="Example"
  code={`import { GlassButton, GlassDropdown } from "liquidify";
import { useState } from "react";
import { GlassButton, GlassDropdown } from 'liquidify'

export default function Demo() {
  const [value, setValue] = useState("");
  const items = [
    { label: "Profile", value: "profile" },
    { label: "Settings", value: "settings" },
    { separator: true },
    { label: "Sign out", value: "logout" },
  ];
  return (
    <div className="space-y-2">
      <GlassDropdown
        trigger={<GlassButton variant="ghost">Open menu</GlassButton>}
        items={items}
        onSelect={setValue}
      />
      <div className="text-white/80 text-sm">Selected: {value || "—"}</div>
    </div>
  );
}`}
  language="tsx"
>
  {(() => {
    
    function Demo() {
      const [value, setValue] = useState("");
      const items = [
        { label: "Profile", value: "profile" },
        { label: "Settings", value: "settings" },
        { separator: true },
        { label: "Sign out", value: "logout" },
      ];
      return (
        <div className="space-y-2">
          <GlassDropdown
            trigger={<GlassButton variant="ghost">Open menu</GlassButton>}
            items={items}
            onSelect={setValue}
          />
          <div className="text-white/80 text-sm">Selected: {value || "—"}</div>
        </div>
      );
    }
    return <Demo />
  })()}
</Preview>

Props

- trigger: ReactNode, items: { label, value, disabled?, icon?, separator? }[], onSelect?: (value) => void
