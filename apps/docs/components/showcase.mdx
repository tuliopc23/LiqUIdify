---
title: "Components Showcase"
description: "Interactive examples of the most commonly used LiqUIdify components with live code previews"
---

# Components Showcase

A curated collection of the most commonly used LiqUIdify components with live snippet previews (rendered) followed by their code blocks. For more examples, visit our [Storybook](https://liquidify-storybook.vercel.app).

import ButtonExample from "/snippets/components/ButtonExample.jsx";
import InputExample from "/snippets/components/InputExample.jsx";
import CardExample from "/snippets/components/CardExample.jsx";
import ModalExample from "/snippets/components/ModalExample.jsx";
import TabsExample from "/snippets/components/TabsExample.jsx";
import TooltipExample from "/snippets/components/TooltipExample.jsx";
import ToastExample from "/snippets/components/ToastExample.jsx";

## Overview

Explore a quick visual overview. Each component is presented with a short description, a live preview, and a usage code block.

### Button

A primary action trigger with hover, focus, and active states.

<ButtonExample />

```tsx title="Usage"
import { Button } from "liquidify";

export default function Example() {
  return <Button variant="primary">Click me</Button>;
}
```

### Input

Text input with clear focus and hover states.

<InputExample />

```tsx title="Usage"
import { Input } from "liquidify";

export default function Example() {
  return <Input placeholder="Type here" />;
}
```

### Card

Container for grouping related content.

<CardExample />

```tsx title="Usage"
import { Card } from "liquidify";

export default function Example() {
  return (
    <Card title="Card title" subtitle="Subtitle">
      Content
    </Card>
  );
}
```

### Modal

Overlay dialog with accessible focus management.

<ModalExample />

```tsx title="Usage"
import { Modal, Button } from "liquidify";
import React from "react";

export default function Example() {
  const [open, setOpen] = React.useState(false);
  return (
    <>
      <Button variant="primary" onClick={() => setOpen(true)}>
        Open modal
      </Button>
      <Modal open={open} onOpenChange={setOpen} title="Modal title">
        <p>Modal content</p>
      </Modal>
    </>
  );
}
```

### Tabs

Switch between content sections.

<TabsExample />

```tsx title="Usage"
import { Tabs } from "liquidify";

export default function Example() {
  return (
    <Tabs
      tabs=[
        { id: "tab1", label: "Tab One", content: <div>Content One</div> },
        { id: "tab2", label: "Tab Two", content: <div>Content Two</div> },
        { id: "tab3", label: "Tab Three", content: <div>Content Three</div> }
      ]
      initialTabId="tab1"
    />
  );
}
```

### Tooltip

Contextual hint shown on hover or focus.

<TooltipExample />

```tsx title="Usage"
import { Tooltip, Button } from "liquidify";

export default function Example() {
  return (
    <Tooltip content="Helpful tip">
      <Button variant="secondary">Hover me</Button>
    </Tooltip>
  );
}
```

### Toast

Transient notification with optional description.

<ToastExample />

```tsx title="Usage"
import { Toast, Button } from "liquidify";
import React from "react";

export default function Example() {
  const [open, setOpen] = React.useState(false);
  return (
    <>
      <Button onClick={() => setOpen(true)}>Show toast</Button>
      <Toast
        open={open}
        onOpenChange={setOpen}
        title="Saved"
        description="Your changes have been saved."
      />
    </>
  );
}
```

## More examples

For a full playground, visit our [Storybook](https://liquidify-storybook.vercel.app).

## Interactive Examples

For live, interactive examples of all these components, visit our [Storybook playground](https://liquidify-storybook.vercel.app) where you can:

- See components in action
- Modify props in real-time
- Test different states and variants
- Copy code snippets
- Test accessibility features

## Component Documentation

Each component has detailed documentation including:

- **Props API**: Complete list of available properties
- **Examples**: Multiple usage scenarios
- **Accessibility**: WCAG guidelines and keyboard navigation
- **Best Practices**: Implementation recommendations

Browse the component documentation in the sidebar to learn more about each component's capabilities and usage patterns.
