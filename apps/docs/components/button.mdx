---
title: Button
description: Interactive buttons with liquid glass effects, multiple variants, and comprehensive accessibility.
icon: mouse-pointer-click
---

# Button

A versatile glassmorphic button component with liquid glass effects, multiple variants, sizes, and comprehensive interaction states. Features Apple-inspired design with smooth animations and full accessibility support.

## Installation

```bash
npm install liquidify
```

## Basic Usage

```tsx
import "liquidify/css";

export default function Demo() {
  return <GlassButton variant="primary">Click me</GlassButton>;
}
```

## Examples

### Basic Variants

```tsx
export default function BasicVariants() {
  return (
    <div className="flex items-center gap-4 flex-wrap">
      <GlassButton variant="primary">Primary</GlassButton>
      <GlassButton variant="secondary">Secondary</GlassButton>
      <GlassButton variant="ghost">Ghost</GlassButton>
      <GlassButton variant="destructive">Destructive</GlassButton>
    </div>
  );
}
```

### Size Variants

```tsx
export default function SizeVariants() {
  return (
    <div className="flex items-center gap-4 flex-wrap">
      <GlassButton size="sm">Small</GlassButton>
      <GlassButton size="md">Medium</GlassButton>
      <GlassButton size="lg">Large</GlassButton>
      <GlassButton size="xl">Extra Large</GlassButton>
    </div>
  );
}
```

### With Icons

```tsx
export default function WithIcons() {
  return (
    <div className="flex items-center gap-4 flex-wrap">
      <GlassButton leftIcon={<Heart className="w-4 h-4" />}>Like</GlassButton>
      <GlassButton rightIcon={<Download className="w-4 h-4" />}>
        Download
      </GlassButton>
      <GlassButton
        leftIcon={<Plus className="w-4 h-4" />}
        rightIcon={<ArrowRight className="w-4 h-4" />}
      >
        Add & Continue
      </GlassButton>
    </div>
  );
}
```

### Loading States

```tsx
export default function LoadingStates() {
  const [loading, setLoading] = useState(false);

  const handleClick = () => {
    setLoading(true);
    setTimeout(() => setLoading(false), 2000);
  };

  return (
    <div className="flex items-center gap-4 flex-wrap">
      <GlassButton loading>Loading...</GlassButton>
      <GlassButton loading loadingText="Saving...">
        Save
      </GlassButton>
      <GlassButton
        loading={loading}
        loadingText="Processing..."
        leftIcon={<Save className="w-4 h-4" />}
        onClick={handleClick}
      >
        {loading ? "Processing..." : "Process Data"}
      </GlassButton>
    </div>
  );
}
```

## Props

| Prop          | Type                                                   | Default     | Description                            |
| ------------- | ------------------------------------------------------ | ----------- | -------------------------------------- |
| `variant`     | `"primary" \| "secondary" \| "ghost" \| "destructive"` | `"primary"` | Button style variant                   |
| `size`        | `"sm" \| "md" \| "lg" \| "xl"`                         | `"md"`      | Button size                            |
| `leftIcon`    | `ReactNode`                                            | -           | Icon displayed on the left side        |
| `rightIcon`   | `ReactNode`                                            | -           | Icon displayed on the right side       |
| `loading`     | `boolean`                                              | `false`     | Show loading state with spinner        |
| `loadingText` | `string`                                               | -           | Text to show during loading state      |
| `fullWidth`   | `boolean`                                              | `false`     | Make button full width                 |
| `iconOnly`    | `boolean`                                              | `false`     | Icon-only button (square aspect ratio) |
| `disabled`    | `boolean`                                              | `false`     | Disabled state                         |
| `children`    | `ReactNode`                                            | -           | Button content                         |
| `className`   | `string`                                               | -           | Additional CSS classes                 |
| `onClick`     | `(event) => void`                                      | -           | Click event handler                    |

## Usage Patterns

### Form Actions

```tsx
function FormActions({ onSave, onCancel, isLoading }) {
  return (
    <div className="flex justify-end gap-3">
      <GlassButton variant="ghost" onClick={onCancel} disabled={isLoading}>
        Cancel
      </GlassButton>
      <GlassButton
        variant="primary"
        leftIcon={<Save className="w-4 h-4" />}
        loading={isLoading}
        loadingText="Saving..."
        onClick={onSave}
      >
        Save Changes
      </GlassButton>
    </div>
  );
}
```

## Accessibility

The GlassButton component follows WCAG 2.1 guidelines:

### Keyboard Navigation

- **Tab**: Navigate between buttons
- **Enter/Space**: Activate button
- **Escape**: Cancel action (context-dependent)

### Screen Reader Support

- Proper button semantics
- Loading state announcements
- Disabled state handling
- Icon descriptions when needed

## Best Practices

### Button Labels

- Use action verbs ("Save", "Delete", "Continue")
- Keep labels concise and descriptive
- Avoid generic terms like "Click here"
- Consider internationalization

### Visual Hierarchy

- Use primary buttons for main actions
- Limit primary buttons per view
- Group related actions together
- Maintain consistent spacing

## Storybook

For interactive examples and component playground, visit our [Storybook](https://liquidify-storybook.vercel.app/?path=/docs/components-button--docs).
