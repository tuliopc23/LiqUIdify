---
title: Card
description: Structured content containers with layered liquid glass effects and compound components.
icon: rectangle-horizontal
---

# Card

A versatile glassmorphic card component with layered liquid glass effects and a compound component API. Perfect for displaying structured content with Apple-inspired depth and transparency.

## Live Examples

### Basic Usage

```tsx live
import { GlassCard, GlassButton } from "liquidify";

export default function Demo() {
  return (
    <GlassCard className="max-w-md">
      <GlassCard.Header>
        <GlassCard.Title>Glass Card</GlassCard.Title>
        <GlassCard.Description>
          Apple-inspired layered transparency with smooth glass effects.
        </GlassCard.Description>
      </GlassCard.Header>
      <GlassCard.Content>
        <p className="text-gray-600 dark:text-gray-300">
          Use cards for sections, panels, and feature blocks. The liquid glass
          effect creates beautiful depth and visual hierarchy.
        </p>
      </GlassCard.Content>
      <GlassCard.Actions>
        <GlassButton variant="primary">Primary Action</GlassButton>
        <GlassButton variant="ghost">Secondary</GlassButton>
      </GlassCard.Actions>
    </GlassCard>
  );
}
```

### Size Variants

```tsx live
import { GlassCard } from "liquidify";

export default function Demo() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4 w-full">
      <GlassCard size="sm">
        <GlassCard.Header>
          <GlassCard.Title>Small</GlassCard.Title>
        </GlassCard.Header>
        <GlassCard.Content>
          <p className="text-sm">Compact card with minimal padding.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard size="md">
        <GlassCard.Header>
          <GlassCard.Title>Medium</GlassCard.Title>
        </GlassCard.Header>
        <GlassCard.Content>
          <p>Default size with standard spacing.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard size="lg">
        <GlassCard.Header>
          <GlassCard.Title>Large</GlassCard.Title>
        </GlassCard.Header>
        <GlassCard.Content>
          <p className="text-lg">Spacious layout with generous padding.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard size="xl">
        <GlassCard.Header>
          <GlassCard.Title>Extra Large</GlassCard.Title>
        </GlassCard.Header>
        <GlassCard.Content>
          <p className="text-xl">Maximum space for important content.</p>
        </GlassCard.Content>
      </GlassCard>
    </div>
  );
}
```

### Style Variants

```tsx live
import { GlassCard } from "liquidify";

export default function Demo() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6 w-full">
      <GlassCard variant="default">
        <GlassCard.Header>
          <GlassCard.Title>Default</GlassCard.Title>
          <GlassCard.Description>Standard glass effect</GlassCard.Description>
        </GlassCard.Header>
        <GlassCard.Content>
          <p>Base glass styling with subtle transparency.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard variant="elevated">
        <GlassCard.Header>
          <GlassCard.Title>Elevated</GlassCard.Title>
          <GlassCard.Description>
            Enhanced depth and shadow
          </GlassCard.Description>
        </GlassCard.Header>
        <GlassCard.Content>
          <p>More prominent with enhanced glass effects.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard variant="outlined">
        <GlassCard.Header>
          <GlassCard.Title>Outlined</GlassCard.Title>
          <GlassCard.Description>Visible border emphasis</GlassCard.Description>
        </GlassCard.Header>
        <GlassCard.Content>
          <p>Clear border with glass background.</p>
        </GlassCard.Content>
      </GlassCard>

      <GlassCard variant="interactive">
        <GlassCard.Header>
          <GlassCard.Title>Interactive</GlassCard.Title>
          <GlassCard.Description>
            Optimized for clickable cards
          </GlassCard.Description>
        </GlassCard.Header>
        <GlassCard.Content>
          <p>Hover and focus states for interactions.</p>
        </GlassCard.Content>
      </GlassCard>
    </div>
  );
}
```

### Interactive Cards

```tsx live
import { useState } from "react";
import { GlassCard, GlassBadge } from "liquidify";

export default function Demo() {
  const [selected, setSelected] = useState(null);

  const cards = [
    {
      id: 1,
      title: "Basic Plan",
      price: "$9",
      features: ["5 Projects", "10GB Storage"],
    },
    {
      id: 2,
      title: "Pro Plan",
      price: "$29",
      features: ["Unlimited Projects", "100GB Storage"],
    },
    {
      id: 3,
      title: "Enterprise",
      price: "$99",
      features: ["Everything", "Priority Support"],
    },
  ];

  return (
    <div className="grid grid-cols-1 md:grid-cols-3 gap-4 w-full">
      {cards.map((card) => (
        <GlassCard
          key={card.id}
          variant="interactive"
          interactive
          selected={selected === card.id}
          onClick={() => setSelected(card.id)}
          className="cursor-pointer"
        >
          <GlassCard.Header>
            <div className="flex items-center justify-between">
              <GlassCard.Title>{card.title}</GlassCard.Title>
              {card.id === 2 && (
                <GlassBadge variant="success">Popular</GlassBadge>
              )}
            </div>
            <div className="text-2xl font-bold">
              {card.price}
              <span className="text-sm font-normal">/month</span>
            </div>
          </GlassCard.Header>
          <GlassCard.Content>
            <ul className="space-y-2">
              {card.features.map((feature, index) => (
                <li key={index} className="flex items-center gap-2">
                  <span className="text-green-500">✓</span>
                  {feature}
                </li>
              ))}
            </ul>
          </GlassCard.Content>
        </GlassCard>
      ))}
    </div>
  );
}
```

### Rich Content

```tsx live
import { GlassCard, GlassButton, GlassAvatar, GlassBadge } from "liquidify";
import { Calendar, MapPin, Users } from "lucide-react";

export default function Demo() {
  return (
    <div className="grid grid-cols-1 md:grid-cols-2 gap-6 w-full">
      <GlassCard>
        <GlassCard.Header>
          <div className="flex items-start gap-3">
            <GlassAvatar
              src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
              alt="John Doe"
              fallback="JD"
            />
            <div className="flex-1">
              <GlassCard.Title>John Doe</GlassCard.Title>
              <GlassCard.Description>Senior Developer</GlassCard.Description>
              <div className="flex gap-2 mt-2">
                <GlassBadge variant="success">Available</GlassBadge>
                <GlassBadge variant="default">Remote</GlassBadge>
              </div>
            </div>
          </div>
        </GlassCard.Header>
        <GlassCard.Content>
          <p className="text-sm text-gray-600 dark:text-gray-300">
            Experienced full-stack developer with expertise in React, Node.js,
            and cloud technologies.
          </p>
        </GlassCard.Content>
        <GlassCard.Actions>
          <GlassButton variant="primary" size="sm">
            Message
          </GlassButton>
          <GlassButton variant="ghost" size="sm">
            View Profile
          </GlassButton>
        </GlassCard.Actions>
      </GlassCard>

      <GlassCard>
        <GlassCard.Header>
          <GlassCard.Title>Team Meeting</GlassCard.Title>
          <GlassCard.Description>
            Weekly sync and planning session
          </GlassCard.Description>
        </GlassCard.Header>
        <GlassCard.Content>
          <div className="space-y-3">
            <div className="flex items-center gap-2 text-sm">
              <Calendar className="w-4 h-4 text-gray-500" />
              <span>Today, 2:00 PM - 3:00 PM</span>
            </div>
            <div className="flex items-center gap-2 text-sm">
              <MapPin className="w-4 h-4 text-gray-500" />
              <span>Conference Room A</span>
            </div>
            <div className="flex items-center gap-2 text-sm">
              <Users className="w-4 h-4 text-gray-500" />
              <span>8 attendees</span>
            </div>
          </div>
        </GlassCard.Content>
        <GlassCard.Footer>
          <div className="flex -space-x-2">
            <GlassAvatar size="sm" fallback="AB" />
            <GlassAvatar size="sm" fallback="CD" />
            <GlassAvatar size="sm" fallback="EF" />
            <div className="w-8 h-8 rounded-full bg-gray-200 dark:bg-gray-700 flex items-center justify-center text-xs">
              +5
            </div>
          </div>
        </GlassCard.Footer>
      </GlassCard>
    </div>
  );
}
```

## Props

### GlassCard

| Prop          | Type                                                     | Default     | Description                              |
| ------------- | -------------------------------------------------------- | ----------- | ---------------------------------------- |
| `size`        | `"sm" \| "md" \| "lg" \| "xl"`                           | `"md"`      | Card size affecting padding and spacing  |
| `variant`     | `"default" \| "elevated" \| "outlined" \| "interactive"` | `"default"` | Visual style variant                     |
| `interactive` | `boolean`                                                | `false`     | Enable interactive states (hover, focus) |
| `selected`    | `boolean`                                                | `false`     | Selected state styling                   |
| `children`    | `ReactNode`                                              | -           | **Required.** Card content               |
| `className`   | `string`                                                 | -           | Additional CSS classes                   |
| `onClick`     | `(event) => void`                                        | -           | Click event handler                      |

### Card Sub-components

All sub-components (`Header`, `Title`, `Description`, `Content`, `Footer`, `Actions`) accept:

| Prop        | Type        | Default | Description                            |
| ----------- | ----------- | ------- | -------------------------------------- |
| `children`  | `ReactNode` | -       | Component content                      |
| `className` | `string`    | -       | Additional CSS classes                 |
| `as`        | `string`    | -       | HTML element to render as (Title only) |

## Usage Patterns

### Product Cards

```tsx
import { GlassCard, GlassButton, GlassBadge } from "liquidify";
import { Star } from "lucide-react";

function ProductCard({ product }) {
  return (
    <GlassCard>
      <GlassCard.Header>
        <div className="aspect-video bg-gray-200 rounded-lg mb-3">
          <img
            src={product.image}
            alt={product.name}
            className="w-full h-full object-cover rounded-lg"
          />
        </div>
        <div className="flex items-start justify-between">
          <div className="flex-1">
            <GlassCard.Title>{product.name}</GlassCard.Title>
            <div className="flex items-center gap-1 mt-1">
              <Star className="w-4 h-4 fill-yellow-400 text-yellow-400" />
              <span className="text-sm">{product.rating}</span>
              <span className="text-sm text-gray-500">({product.reviews})</span>
            </div>
          </div>
          {product.isNew && <GlassBadge variant="success">New</GlassBadge>}
        </div>
      </GlassCard.Header>
      <GlassCard.Content>
        <p className="text-sm text-gray-600 mb-3">{product.description}</p>
        <div className="flex items-center justify-between">
          <span className="text-lg font-bold">${product.price}</span>
          {product.originalPrice && (
            <span className="text-sm text-gray-500 line-through">
              ${product.originalPrice}
            </span>
          )}
        </div>
      </GlassCard.Content>
      <GlassCard.Actions>
        <GlassButton variant="primary" fullWidth>
          Add to Cart
        </GlassButton>
      </GlassCard.Actions>
    </GlassCard>
  );
}
```

### Dashboard Widgets

```tsx
import { GlassCard, GlassBadge } from "liquidify";
import { TrendingUp, TrendingDown } from "lucide-react";

function MetricCard({ metric }) {
  const isPositive = metric.change > 0;

  return (
    <GlassCard size="sm">
      <GlassCard.Header>
        <div className="flex items-center justify-between">
          <GlassCard.Title className="text-sm">{metric.label}</GlassCard.Title>
          <GlassBadge variant={isPositive ? "success" : "error"}>
            {isPositive ? "+" : ""}
            {metric.change}%
          </GlassBadge>
        </div>
      </GlassCard.Header>
      <GlassCard.Content>
        <div className="flex items-center gap-2">
          <span className="text-2xl font-bold">{metric.value}</span>
          {isPositive ? (
            <TrendingUp className="w-5 h-5 text-green-500" />
          ) : (
            <TrendingDown className="w-5 h-5 text-red-500" />
          )}
        </div>
        <p className="text-xs text-gray-500 mt-1">vs last period</p>
      </GlassCard.Content>
    </GlassCard>
  );
}
```

### Settings Panels

```tsx
import { GlassCard, GlassButton, GlassInput, GlassCheckbox } from "liquidify";

function SettingsCard({ title, description, children, onSave, onReset }) {
  return (
    <GlassCard>
      <GlassCard.Header>
        <GlassCard.Title>{title}</GlassCard.Title>
        <GlassCard.Description>{description}</GlassCard.Description>
      </GlassCard.Header>
      <GlassCard.Content>{children}</GlassCard.Content>
      <GlassCard.Actions>
        <GlassButton variant="ghost" onClick={onReset}>
          Reset
        </GlassButton>
        <GlassButton variant="primary" onClick={onSave}>
          Save Changes
        </GlassButton>
      </GlassCard.Actions>
    </GlassCard>
  );
}
```

## Accessibility

The GlassCard component follows WCAG 2.1 guidelines:

### Semantic Structure

- Proper heading hierarchy with Title components
- Logical content organization
- Clear visual hierarchy
- Meaningful content grouping

### Interactive Cards

- Keyboard navigation support
- Focus indicators for clickable cards
- Proper ARIA attributes
- Screen reader friendly structure

### Visual Accessibility

- High contrast ratios
- Clear visual boundaries
- Sufficient spacing between elements
- Readable typography

## Apple HIG Guidelines

Following Apple's Human Interface Guidelines for content containers:

### Visual Design

- **Clarity**: Clean content organization and typography
- **Depth**: Layered glass effects create visual hierarchy
- **Consistency**: Uniform styling across card variants

### Content

- **Scannable**: Easy to scan and understand
- **Organized**: Logical content structure
- **Focused**: Single purpose per card

### Interaction

- **Responsive**: Immediate feedback for interactive cards
- **Predictable**: Consistent behavior patterns
- **Contextual**: Appropriate for the content type

## Best Practices

### Content Organization

- Use clear, descriptive titles
- Keep descriptions concise
- Group related information
- Maintain consistent content structure

### Visual Hierarchy

- Use appropriate card sizes
- Limit content density
- Maintain consistent spacing
- Consider card relationships

### Interactive Design

- Make clickable cards obvious
- Provide clear hover states
- Use appropriate cursor styles
- Consider touch targets on mobile

### Performance

- Avoid heavy content in cards
- Consider lazy loading for images
- Optimize for large lists
- Use appropriate card variants

## Examples

### Article Preview

```tsx
function ArticleCard({ article }) {
  return (
    <GlassCard variant="interactive" interactive>
      <GlassCard.Header>
        <div className="aspect-video bg-gray-200 rounded-lg mb-3">
          <img
            src={article.coverImage}
            alt={article.title}
            className="w-full h-full object-cover rounded-lg"
          />
        </div>
        <GlassCard.Title>{article.title}</GlassCard.Title>
        <GlassCard.Description>{article.excerpt}</GlassCard.Description>
      </GlassCard.Header>
      <GlassCard.Content>
        <div className="flex items-center gap-3">
          <GlassAvatar
            src={article.author.avatar}
            alt={article.author.name}
            size="sm"
          />
          <div className="text-sm">
            <div className="font-medium">{article.author.name}</div>
            <div className="text-gray-500">{article.publishedAt}</div>
          </div>
        </div>
      </GlassCard.Content>
      <GlassCard.Footer>
        <div className="flex items-center justify-between">
          <div className="flex gap-2">
            {article.tags.map((tag) => (
              <GlassBadge key={tag} variant="default">
                {tag}
              </GlassBadge>
            ))}
          </div>
          <span className="text-sm text-gray-500">
            {article.readTime} min read
          </span>
        </div>
      </GlassCard.Footer>
    </GlassCard>
  );
}
```
