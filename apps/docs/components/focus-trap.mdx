---
title: Focus Trap
description: Utility to lock keyboard focus within a region.
---

import Preview from "./Preview";
import * as React from "react";
import { GlassButton, GlassFocusTrap } from "liquidify";

# Focus Trap

<Preview
  title="Example"
  code={`import { GlassButton, GlassFocusTrap } from "liquidify";
import { useState } from "react";

export default function Demo() {
  const [open, setOpen] = useState(false);
  return (
    <div>
      <GlassButton onClick={() => setOpen((o) => !o)}>
        {open ? "Close" : "Open"} Panel
      </GlassButton>
      {open && (
        <GlassFocusTrap>
          <div className="p-4 bg-liquid-readable rounded-xl space-y-2">
            <input className="liquid-glass-input" placeholder="First" />
            <input className="liquid-glass-input" placeholder="Second" />
            <GlassButton onClick={() => setOpen(false)}>Done</GlassButton>
          </div>
        </GlassFocusTrap>
      )}
    </div>
  );
}`}
  language="tsx"
>
  {(() => {
    
    function Demo() {
      const [open, setOpen] = useState(false);
      return (
        <div>
          <GlassButton onClick={() => setOpen((o) => !o)}>
            {open ? "Close" : "Open"} Panel
          </GlassButton>
          {open && (
            <GlassFocusTrap>
              <div className="p-4 bg-liquid-readable rounded-xl space-y-2">
                <input className="liquid-glass-input" placeholder="First" />
                <input className="liquid-glass-input" placeholder="Second" />
                <GlassButton onClick={() => setOpen(false)}>Done</GlassButton>
              </div>
            </GlassFocusTrap>
          )}
        </div>
      );
    }
    return <Demo />
  })()}
</Preview>

## Notes

Focus is restored to trigger element when trap unmounts.
