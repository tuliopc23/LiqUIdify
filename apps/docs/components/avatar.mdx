---
title: Avatar
description: User profile images with glassmorphism effects, fallbacks, and status indicators.
icon: user-circle
---

# Avatar

A glassmorphic avatar component for displaying user profile images with elegant fallbacks, status indicators, and multiple size variants. Features Apple-inspired design with smooth glass effects.

## Live Examples

### Basic Usage

```tsx
<div className="flex items-center gap-4">
  <GlassAvatar
    src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
    alt="John Doe"
    fallback="JD"
  />
  <GlassAvatar fallback="AB" alt="Alice Brown" />
  <GlassAvatar />
</div>
```

### Size Variants

```tsx
<div className="flex items-center gap-4">
  <GlassAvatar
    size="xs"
    src="https://images.unsplash.com/photo-1494790108755-2616b612b786?w=100&h=100&fit=crop&crop=face"
    alt="Extra Small"
  />
  <GlassAvatar
    size="sm"
    src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face"
    alt="Small"
  />
  <GlassAvatar
    size="md"
    src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
    alt="Medium"
  />
  <GlassAvatar
    size="lg"
    src="https://images.unsplash.com/photo-1500648767791-00dcc994a43e?w=100&h=100&fit=crop&crop=face"
    alt="Large"
  />
  <GlassAvatar
    size="xl"
    src="https://images.unsplash.com/photo-1534528741775-53994a69daeb?w=100&h=100&fit=crop&crop=face"
    alt="Extra Large"
  />
  <GlassAvatar
    size="2xl"
    src="https://images.unsplash.com/photo-1517841905240-472988babdf9?w=100&h=100&fit=crop&crop=face"
    alt="2X Large"
  />
</div>
```

### Shape Variants

```tsx
export default function Demo() {
  return (
    <div className="flex items-center gap-4">
      <div className="text-center space-y-2">
        <GlassAvatar
          variant="circular"
          src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
          alt="Circular"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Circular</p>
      </div>
      <div className="text-center space-y-2">
        <GlassAvatar
          variant="rounded"
          src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face"
          alt="Rounded"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Rounded</p>
      </div>
      <div className="text-center space-y-2">
        <GlassAvatar
          variant="square"
          src="https://images.unsplash.com/photo-1494790108755-2616b612b786?w=100&h=100&fit=crop&crop=face"
          alt="Square"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Square</p>
      </div>
    </div>
  );
}
```

### Status Indicators

```tsx
export default function Demo() {
  return (
    <div className="flex items-center gap-6">
      <div className="text-center space-y-2">
        <GlassAvatar
          src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
          alt="Online User"
          status="online"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Online</p>
      </div>
      <div className="text-center space-y-2">
        <GlassAvatar
          src="https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&crop=face"
          alt="Away User"
          status="away"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Away</p>
      </div>
      <div className="text-center space-y-2">
        <GlassAvatar
          src="https://images.unsplash.com/photo-1494790108755-2616b612b786?w=100&h=100&fit=crop&crop=face"
          alt="Busy User"
          status="busy"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Busy</p>
      </div>
      <div className="text-center space-y-2">
        <GlassAvatar
          fallback="OF"
          alt="Offline User"
          status="offline"
          size="lg"
        />
        <p className="text-xs text-[var(--apple-text-secondary)]">Offline</p>
      </div>
    </div>
  );
}
```

### With Border

```tsx
export default function Demo() {
  return (
    <div className="flex items-center gap-4">
      <GlassAvatar
        src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?w=100&h=100&fit=crop&crop=face"
        alt="With Border"
        showBorder
        size="lg"
      />
      <GlassAvatar
        fallback="AB"
        alt="Fallback with Border"
        showBorder
        size="lg"
      />
      <GlassAvatar showBorder size="lg" />
    </div>
  );
}
```

## Props

| Prop         | Type                                            | Default      | Description                      |
| ------------ | ----------------------------------------------- | ------------ | -------------------------------- |
| `src`        | `string`                                        | -            | Image source URL                 |
| `alt`        | `string`                                        | -            | Alt text for the image           |
| `size`       | `"xs" \| "sm" \| "md" \| "lg" \| "xl" \| "2xl"` | `"md"`       | Avatar size                      |
| `variant`    | `"circular" \| "rounded" \| "square"`           | `"circular"` | Avatar shape                     |
| `fallback`   | `string`                                        | -            | Fallback text (usually initials) |
| `showBorder` | `boolean`                                       | `false`      | Show glass border effect         |
| `status`     | `"online" \| "offline" \| "away" \| "busy"`     | -            | Status indicator                 |
| `className`  | `string`                                        | -            | Additional CSS classes           |

## Usage Patterns

### User Profile

```tsx
function UserProfile({ user }) {
  return (
    <div className="flex items-center gap-3">
      <GlassAvatar
        src={user.avatar}
        alt={user.name}
        fallback={user.initials}
        status={user.status}
        size="lg"
        showBorder
      />
      <div>
        <h3 className="font-semibold">{user.name}</h3>
        <p className="text-sm text-[var(--apple-text-secondary)]">{user.role}</p>
      </div>
    </div>
  );
}
```

### Team Members List

```tsx
function TeamMembersList({ members }) {
  return (
    <div className="space-y-4">
      {members.map((member) => (
        <div
          key={member.id}
          className="flex items-center justify-between p-4 rounded-lg bg-white/5"
        >
          <div className="flex items-center gap-3">
            <GlassAvatar
              src={member.avatar}
              alt={member.name}
              fallback={member.initials}
              status={member.status}
            />
            <div>
              <h4 className="font-medium">{member.name}</h4>
              <p className="text-sm text-[var(--apple-text-secondary)]">{member.email}</p>
            </div>
          </div>
          <span className="text-sm text-[var(--apple-text-secondary)]">{member.role}</span>
        </div>
      ))}
    </div>
  );
}
```

### Avatar Group

```tsx
function AvatarGroup({ users, max = 3 }) {
  const displayUsers = users.slice(0, max);
  const remainingCount = users.length - max;

  return (
    <div className="flex -space-x-2">
      {displayUsers.map((user, index) => (
        <GlassAvatar
          key={user.id}
          src={user.avatar}
          alt={user.name}
          fallback={user.initials}
          showBorder
          className="relative z-10"
          style={{ zIndex: displayUsers.length - index }}
        />
      ))}
      {remainingCount > 0 && (
        <GlassAvatar
          fallback={`+${remainingCount}`}
          showBorder
          className="relative z-0"
        />
      )}
    </div>
  );
}
```

### Comment System

```tsx
function Comment({ comment }) {
  return (
    <div className="flex gap-3">
      <GlassAvatar
        src={comment.author.avatar}
        alt={comment.author.name}
        fallback={comment.author.initials}
        size="sm"
      />
      <div className="flex-1">
        <div className="flex items-center gap-2 mb-1">
          <span className="font-medium text-sm">{comment.author.name}</span>
          <span className="text-xs text-[var(--apple-text-secondary)]">{comment.timestamp}</span>
        </div>
        <p className="text-sm">{comment.content}</p>
      </div>
    </div>
  );
}
```

## Accessibility

The GlassAvatar component follows WCAG 2.1 guidelines:

### Image Accessibility

- Proper `alt` text for profile images
- Fallback content for failed image loads
- Meaningful initials when no image available

- Color is not the only indicator of status
- Status information available to screen readers
- Sufficient contrast for status indicators

### Focus Management

- Focusable when interactive (clickable)
- Clear focus indicators
- Logical tab order in groups

## Apple HIG Guidelines

Following Apple's Human Interface Guidelines for user representation:

### Visual Design

- **Clarity**: Clean, recognizable user representation
- **Depth**: Subtle glass effects create visual depth
- **Consistency**: Uniform sizing and styling

### Content

- **Personal**: Use actual user photos when available
- **Fallback**: Meaningful initials or icons
- **Respectful**: Appropriate image handling and privacy

### Interaction

- **Responsive**: Immediate feedback on interaction
- **Contextual**: Appropriate for the use case
- **Accessible**: Works for all users

## Best Practices

### Image Handling

- Provide meaningful alt text
- Use appropriate image sizes (avoid large images)
- Handle loading states gracefully
- Implement error fallbacks

### Fallback Content

- Use user initials when possible
- Ensure initials are readable
- Consider cultural naming conventions
- Provide generic fallback for edge cases

- Use consistent status meanings
- Don't rely solely on color
- Update status in real-time when possible
- Consider privacy implications

### Performance

- Optimize image sizes
- Use appropriate image formats
- Consider lazy loading for lists
- Cache images when possible

## Examples

### User Dropdown Menu

```tsx
function UserDropdown({ user }) {
  return (
    <div className="relative">
      <button className="flex items-center gap-2 p-2 rounded-lg hover:bg-white/10">
        <GlassAvatar
          src={user.avatar}
          alt={user.name}
          fallback={user.initials}
          size="sm"
          status={user.status}
        />
        <span className="text-sm font-medium">{user.name}</span>
      </button>
      {/* Dropdown menu */}
    </div>
  );
}
```

### Message Thread

```tsx
function MessageThread({ messages }) {
  return (
    <div className="space-y-4">
      {messages.map((message) => (
        <div key={message.id} className="flex gap-3">
          <GlassAvatar
            src={message.sender.avatar}
            alt={message.sender.name}
            fallback={message.sender.initials}
            size="sm"
          />
          <div className="flex-1">
            <div className="bg-white/10 rounded-lg p-3">
              <div className="flex items-center gap-2 mb-1">
                <span className="font-medium text-sm">
                  {message.sender.name}
                </span>
                <span className="text-xs text-[var(--apple-text-secondary)]">{message.time}</span>
              </div>
              <p className="text-sm">{message.content}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  );
}
```

### Profile Card

```tsx
function ProfileCard({ user }) {
  return (
    <div className="text-center p-6 bg-white/10 rounded-xl">
      <GlassAvatar
        src={user.avatar}
        alt={user.name}
        fallback={user.initials}
        size="2xl"
        showBorder
        status={user.status}
        className="mx-auto mb-4"
      />
      <h3 className="font-semibold text-lg mb-1">{user.name}</h3>
      <p className="text-[var(--apple-text-secondary)] text-sm mb-2">{user.role}</p>
      <p className="text-[var(--apple-text-secondary)] text-xs">{user.department}</p>
    </div>
  );
}
```
