---
title: Input
description: Text input fields with liquid glass effects, validation states, and comprehensive accessibility.
icon: type
---

import InputExample from "/snippets/components/InputExample.jsx";

# Input

<!-- STATIC_PREVIEW:input:start -->

<div id="liquidify-static-input-preview">
  <style>
#liquidify-static-input-preview, #liquidify-static-input-preview{
        height: 100%;
        margin: 0;
      }#liquidify-static-input-preview{
        font-family:
          system-ui,
          -apple-system,
          Segoe UI,
          Roboto,
          Helvetica,
          Arial,
          sans-serif;
        background:
          radial-gradient(
            80rem 60rem at 20% 20%,
            rgba(162, 210, 255, 0.28),
            transparent 60%
          ),
          radial-gradient(
            80rem 60rem at 80% 30%,
            rgba(255, 183, 248, 0.22),
            transparent 60%
          ),
          linear-gradient(135deg, #c9e7ff 0%, #d9d0ff 45%, #ffc9dc 100%);
        display: grid;
        place-items: center;
        padding: 24px;
        color: #0f172a;
      }#liquidify-static-input-preview /* Tokens */ #liquidify-static-input-preview{
        --lg-text: #0f172a;
        --lg-text-subtle: #334155;
        --lg-text-inverse: #ffffff;

        --lg-border: rgba(255, 255, 255, 0.22);
        --lg-border-strong: rgba(255, 255, 255, 0.32);
        --lg-bg-tint: rgba(255, 255, 255, 0.05);

        --lg-primary: #007aff;
        --lg-success: #16a34a;
        --lg-error: #ef4444;
        --lg-warning: #d97706;

        --lg-shadow:
          0 12px 22px rgba(0, 0, 0, 0.18), 0 2px 6px rgba(0, 0, 0, 0.1);

        --lg-radius-sm: 10px;
        --lg-radius-md: 12px;

        --lg-transition: 160ms cubic-bezier(0.2, 0.8, 0.2, 1);
      }#liquidify-static-input-preview .theme-dark{
        --lg-text: #f8fafc;
        --lg-text-subtle: #cbd5e1;
        --lg-border: rgba(255, 255, 255, 0.18);
        --lg-border-strong: rgba(255, 255, 255, 0.3);
        --lg-bg-tint: rgba(255, 255, 255, 0.03);
      }#liquidify-static-input-preview /* Layout helpers */ .canvas{
        width: 100%;
        max-width: 1000px;
        display: grid;
        gap: 18px;
      }#liquidify-static-input-preview .row{
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
        align-items: center;
      }#liquidify-static-input-preview .label{
        color: #111827cc;
        font-weight: 600;
        letter-spacing: 0.02em;
      }#liquidify-static-input-preview .theme-dark .label{
        color: #e5e7ebcc;
      }#liquidify-static-input-preview .stack{
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
        gap: 16px;
        width: 100%;
      }#liquidify-static-input-preview /* Field block: label + control + message */ .field-block{
        display: grid;
        gap: 8px;
        min-width: 0;
      }#liquidify-static-input-preview .field-label{
        font-size: 12px;
        font-weight: 600;
        letter-spacing: 0.015em;
        color: var(--lg-text-subtle);
      }#liquidify-static-input-preview .field-msg{
        font-size: 12px;
        line-height: 1.3;
        color: var(--lg-text-subtle);
        min-height: 1em;
      }#liquidify-static-input-preview .field-msg.success{
        color: #16a34a;
      }#liquidify-static-input-preview .field-msg.error{
        color: #ef4444;
      }#liquidify-static-input-preview .field-msg.warning{
        color: #d97706;
      }#liquidify-static-input-preview /* Liquid-glass field wrapper */ .field{
        position: relative;
        border-radius: var(--lg-radius-md);
        border: 1px solid var(--lg-border);
        background: transparent;
        overflow: hidden;
        isolation: isolate;
        box-shadow: var(--lg-shadow);
        transition:
          border-color var(--lg-transition),
          box-shadow var(--lg-transition);
      }#liquidify-static-input-preview .field::before{
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        backdrop-filter: blur(12px) saturate(175%);
        -webkit-backdrop-filter: blur(12px) saturate(175%);
        background: var(--lg-bg-tint);
        z-index: -2;
        pointer-events: none;
        transform: translateZ(0);
      }#liquidify-static-input-preview .field::after{
        content: "";
        position: absolute;
        inset: 0;
        border-radius: inherit;
        background: linear-gradient(
          to bottom,
          rgba(255, 255, 255, 0.16),
          rgba(255, 255, 255, 0.07)
        );
        filter: brightness(1.04);
        opacity: 0.55;
        z-index: -1;
        pointer-events: none;
      }#liquidify-static-input-preview /* Input element */ .input{
        appearance: none;
        -webkit-appearance: none;
        width: 100%;
        min-width: 0;
        padding: 12px 12px 12px 40px;
        background: transparent;
        border: none;
        outline: none;
        color: var(--lg-text);
        font-size: 14px;
        font-weight: 500;
        letter-spacing: 0.01em;
        caret-color: var(--lg-primary);
      }#liquidify-static-input-preview .input::placeholder{
        color: rgba(51, 65, 85, 0.7);
      }#liquidify-static-input-preview /* Leading icon (decorative) */ .icon{
        position: absolute;
        left: 12px;
        top: 50%;
        transform: translateY(-50%);
        width: 18px;
        height: 18px;
        opacity: 0.9;
        color: var(--lg-text-subtle);
        pointer-events: none;
      }#liquidify-static-input-preview .icon svg{
        display: block;
        width: 100%;
        height: 100%;
      }#liquidify-static-input-preview /* Focus ring (pure CSS) */ .focus-ring{
        position: absolute;
        inset: -3px;
        border-radius: calc(var(--lg-radius-md) + 3px);
        pointer-events: none;
        opacity: 0;
        transition: opacity var(--lg-transition);
        box-shadow:
          0 0 0 3px rgba(0, 122, 255, 0.15),
          0 0 0 1px rgba(0, 122, 255, 0.6) inset;
      }#liquidify-static-input-preview .input:focus-visible ~ .focus-ring{
        opacity: 1;
      }#liquidify-static-input-preview /* Semantic states */ .field.is-success{
        border-color: rgba(22, 163, 74, 0.6);
      }#liquidify-static-input-preview .field.is-error{
        border-color: rgba(239, 68, 68, 0.65);
      }#liquidify-static-input-preview .field.is-warning{
        border-color: rgba(217, 119, 6, 0.65);
      }#liquidify-static-input-preview /* Disabled / read-only */ .input:disabled{
        opacity: 0.7;
        cursor: not-allowed;
      }#liquidify-static-input-preview .input[readonly]{
        opacity: 0.95;
      }#liquidify-static-input-preview /* Demo surface container (kept translucent) */ .surface{
        padding: 16px;
        border-radius: 16px;
        border: 1px solid rgba(255, 255, 255, 0.12);
        background: rgba(255, 255, 255, 0.06);
        backdrop-filter: blur(6px);
        -webkit-backdrop-filter: blur(6px);
      }#liquidify-static-input-preview /* Mobile tweaks */ @media (max-width: 480px){
        .surface {
          padding: 12px;
        }
        .input {
          font-size: 13.5px;
          padding: 10px 10px 10px 38px;
        }
      }#liquidify-static-input-preview /* Reduced motion */ @media (prefers-reduced-motion: reduce){
        .field,
        .focus-ring {
          transition: none;
        }
      }

  </style>
  <div class="canvas">
      <div class="row">
        <span class="label">Liquid Glass Input — Dark Mode</span>
      </div>

      <div class="theme-dark surface">
        <div
          class="stack"
          role="group"
          aria-label="Static liquid glass input preview (dark)"
        >
          <!-- Default -->
          <div class="field-block">
            <label class="field-label" for="dark-name">Name</label>
            <div class="field">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                  <circle cx="12" cy="7" r="4" />
                </svg>
              </span>
              <input
                id="dark-name"
                class="input"
                type="text"
                placeholder="Enter your name"
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg" aria-live="polite"></div>
          </div>

          <!-- Email (success) -->
          <div class="field-block">
            <label class="field-label" for="dark-email">Email Address</label>
            <div class="field is-success">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M4 8l8 5 8-5" />
                  <rect x="4" y="4" width="16" height="16" rx="2" ry="2"></rect>
                </svg>
              </span>
              <input
                id="dark-email"
                class="input"
                type="email"
                placeholder="name@domain.com"
                value="hello@liquidify.dev"
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg success" aria-live="polite">Looks good</div>
          </div>

          <!-- Password (error) -->
          <div class="field-block">
            <label class="field-label" for="dark-pass">Password</label>
            <div class="field is-error">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M12 9v4m0 4h.01" />
                  <path
                    d="M10.29 3.86l-7.5 12.99A2 2 0 004.5 20h15a2 2 0 001.71-3.15l-7.5-12.99a2 2 0 00-3.42 0z"
                  />
                </svg>
              </span>
              <input
                id="dark-pass"
                class="input"
                type="password"
                placeholder="Enter a strong password"
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg error" aria-live="polite">
              This field is required
            </div>
          </div>

          <!-- Disabled -->
          <div class="field-block">
            <label class="field-label" for="dark-disabled">Disabled</label>
            <div class="field">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <circle cx="12" cy="12" r="10" />
                  <path d="M4.93 4.93l14.14 14.14" />
                </svg>
              </span>
              <input
                id="dark-disabled"
                class="input"
                type="text"
                placeholder="Disabled input"
                disabled
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg" aria-live="polite"></div>
          </div>
        </div>
      </div>

      <div class="row">
        <span class="label">Light Mode</span>
      </div>

      <div class="surface">
        <div
          class="stack"
          role="group"
          aria-label="Static liquid glass input preview (light)"
        >
          <!-- Default -->
          <div class="field-block">
            <label class="field-label" for="light-name">Name</label>
            <div class="field">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                  <circle cx="12" cy="7" r="4" />
                </svg>
              </span>
              <input
                id="light-name"
                class="input"
                type="text"
                placeholder="Enter your name"
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg" aria-live="polite"></div>
          </div>

          <!-- Email (read-only) -->
          <div class="field-block">
            <label class="field-label" for="light-email">Email Address</label>
            <div class="field">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <path d="M4 8l8 5 8-5" />
                  <rect x="4" y="4" width="16" height="16" rx="2" ry="2"></rect>
                </svg>
              </span>
              <input
                id="light-email"
                class="input"
                type="email"
                value="read-only@liquidify.dev"
                readonly
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg" aria-live="polite">Read-only value</div>
          </div>

          <!-- Warning -->
          <div class="field-block">
            <label class="field-label" for="light-website">Website</label>
            <div class="field is-warning">
              <span class="icon" aria-hidden="true">
                <svg
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="1.8"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                >
                  <circle cx="12" cy="12" r="10" />
                  <path d="M12 8v4m0 4h.01" />
                </svg>
              </span>
              <input
                id="light-website"
                class="input"
                type="url"
                placeholder="https://"
              />
              <span class="focus-ring" aria-hidden="true"></span>
            </div>
            <div class="field-msg warning" aria-live="polite">
              Please include protocol (e.g., https://)
            </div>
          </div>
        </div>
      </div>
    </div>

</div>
<!-- STATIC_PREVIEW:input:end -->
A glassmorphic input component with liquid glass effects, validation states, and comprehensive accessibility support. Features Apple-inspired design with smooth focus transitions and clear visual feedback.

{/_Live preview_/}

<InputExample />

<RequestExample>

```bash Install
npm install liquidify
```

```tsx Usage
import "liquidify/styles";
import { GlassInput } from "liquidify";

export default function Demo() {
  return <GlassInput placeholder="Enter your name" />;
}
```

```tsx Variants
<GlassInput size="sm" />
<GlassInput size="md" />
<GlassInput size="lg" />
```

</RequestExample>

## Live Examples

### Basic Usage

### Basic Usage

```tsx
export default function Demo() {
  return (
    <div className="w-full max-w-md space-y-4">
      <GlassInput placeholder="Enter your name" />

      <GlassInput
        type="email"
        placeholder="Enter your email"
        label="Email Address"
      />

      <GlassInput
        type="password"
        placeholder="Enter your password"
        label="Password"
      />
    </div>
  );
}
```

### Size Variants

### Size Variants

```tsx
export default function Demo() {
  return (
    <div className="w-full max-w-md space-y-4">
      <GlassInput size="sm" placeholder="Small input" label="Small" />
      <GlassInput size="md" placeholder="Medium input" label="Medium" />
      <GlassInput size="lg" placeholder="Large input" label="Large" />
    </div>
  );
}
```

### Validation States

### Validation States

```tsx
export default function Demo() {
  return (
    <div className="w-full max-w-md space-y-4">
      <GlassInput
        placeholder="Valid input"
        label="Success State"
        state="success"
        message="This field is valid"
        rightIcon={<Check className="w-4 h-4" />}
      />

      <GlassInput
        placeholder="Invalid input"
        label="Error State"
        state="error"
        message="This field is required"
        rightIcon={<AlertCircle className="w-4 h-4" />}
      />

      <GlassInput
        placeholder="Warning input"
        label="Warning State"
        state="warning"
        message="Please double-check this value"
      />
    </div>
  );
}
```

### With Icons

### With Icons

```tsx
export default function Demo() {
  return (
    <div className="w-full max-w-md space-y-4">
      <GlassInput
        placeholder="Search..."
        leftIcon={<Search className="w-4 h-4" />}
      />

      <GlassInput
        type="email"
        placeholder="Email address"
        leftIcon={<Mail className="w-4 h-4" />}
      />

      <GlassInput
        type="password"
        placeholder="Password"
        leftIcon={<Lock className="w-4 h-4" />}
      />

      <GlassInput
        placeholder="Username"
        leftIcon={<User className="w-4 h-4" />}
        rightIcon={<Check className="w-4 h-4 text-green-500" />}
      />
    </div>
  );
}
```

### Disabled and Read-only

### Disabled and Read-only

```tsx
export default function Demo() {
  return (
    <div className="w-full max-w-md space-y-4">
      <GlassInput placeholder="Disabled input" label="Disabled" disabled />

      <GlassInput value="Read-only value" label="Read-only" readOnly />

      <GlassInput
        value="Disabled with value"
        label="Disabled with Value"
        disabled
      />
    </div>
  );
}
```

## Props

| Prop           | Type                                             | Default     | Description            |
| -------------- | ------------------------------------------------ | ----------- | ---------------------- |
| `type`         | `string`                                         | `"text"`    | HTML input type        |
| `size`         | `"sm" \| "md" \| "lg"`                           | `"md"`      | Input size             |
| `label`        | `string`                                         | -           | Input label            |
| `placeholder`  | `string`                                         | -           | Placeholder text       |
| `state`        | `"default" \| "success" \| "error" \| "warning"` | `"default"` | Validation state       |
| `message`      | `string`                                         | -           | Help or error message  |
| `leftIcon`     | `ReactNode`                                      | -           | Icon on the left side  |
| `rightIcon`    | `ReactNode`                                      | -           | Icon on the right side |
| `disabled`     | `boolean`                                        | `false`     | Disabled state         |
| `readOnly`     | `boolean`                                        | `false`     | Read-only state        |
| `required`     | `boolean`                                        | `false`     | Required field         |
| `value`        | `string`                                         | -           | Controlled value       |
| `defaultValue` | `string`                                         | -           | Default value          |
| `onChange`     | `(event) => void`                                | -           | Change event handler   |
| `onFocus`      | `(event) => void`                                | -           | Focus event handler    |
| `onBlur`       | `(event) => void`                                | -           | Blur event handler     |
| `className`    | `string`                                         | -           | Additional CSS classes |

## Usage Patterns

### Form Fields

```tsx
function ContactForm() {
  const [formData, setFormData] = useState({
    name: "",
    email: "",
    message: "",
  });

  const [errors, setErrors] = useState({});

  const handleSubmit = (e) => {
    e.preventDefault();
    // Validation logic
    const newErrors = {};
    if (!formData.name) newErrors.name = "Name is required";
    if (!formData.email) newErrors.email = "Email is required";

    setErrors(newErrors);

    if (Object.keys(newErrors).length === 0) {
      // Submit form
      console.log("Form submitted:", formData);
    }
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <GlassInput
        label="Full Name"
        placeholder="Enter your full name"
        value={formData.name}
        onChange={(e) =>
          setFormData((prev) => ({ ...prev, name: e.target.value }))
        }
        state={errors.name ? "error" : "default"}
        message={errors.name}
        required
      />

      <GlassInput
        type="email"
        label="Email Address"
        placeholder="Enter your email"
        value={formData.email}
        onChange={(e) =>
          setFormData((prev) => ({ ...prev, email: e.target.value }))
        }
        state={errors.email ? "error" : "default"}
        message={errors.email}
        required
      />

      <GlassButton type="submit" variant="primary">
        Submit
      </GlassButton>
    </form>
  );
}
```

### Search Input

```tsx
function SearchInput({ onSearch, placeholder = "Search..." }) {
  const [query, setQuery] = useState("");

  const handleClear = () => {
    setQuery("");
    onSearch("");
  };

  const handleChange = (e) => {
    const value = e.target.value;
    setQuery(value);
    onSearch(value);
  };

  return (
    <GlassInput
      placeholder={placeholder}
      value={query}
      onChange={handleChange}
      leftIcon={<Search className="w-4 h-4" />}
      rightIcon={
        query && (
          <button
            type="button"
            onClick={handleClear}
            className="p-1 hover:bg-gray-100 rounded"
          >
            <X className="w-3 h-3" />
          </button>
        )
      }
    />
  );
}
```

### Password Input with Visibility Toggle

```tsx
function PasswordInput({ label = "Password", ...props }) {
  const [showPassword, setShowPassword] = useState(false);

  return (
    <GlassInput
      type={showPassword ? "text" : "password"}
      label={label}
      leftIcon={<Lock className="w-4 h-4" />}
      rightIcon={
        <button
          type="button"
          onClick={() => setShowPassword(!showPassword)}
          className="p-1 hover:bg-gray-100 rounded"
        >
          {showPassword ? (
            <EyeOff className="w-4 h-4" />
          ) : (
            <Eye className="w-4 h-4" />
          )}
        </button>
      }
      {...props}
    />
  );
}
```

## Accessibility

The GlassInput component follows WCAG 2.1 guidelines:

### Keyboard Navigation

- **Tab**: Navigate between inputs
- **Enter**: Submit form (when in form)
- **Escape**: Clear input (when applicable)

### Screen Reader Support

- Proper label association
- Error message announcements
- State change notifications
- Required field indicators

### Visual Accessibility

- High contrast focus indicators
- Clear error states
- Sufficient color contrast
- Readable typography

### ARIA Attributes

- `aria-invalid` for error states
- `aria-describedby` for help text
- `aria-required` for required fields
- `aria-label` when no visible label

## Apple HIG Guidelines

Following Apple's Human Interface Guidelines for text fields:

### Visual Design

- **Clarity**: Clean, readable input styling
- **Depth**: Subtle glass effects create visual hierarchy
- **Consistency**: Uniform styling across states

### Interaction

- **Responsiveness**: Immediate visual feedback
- **Predictability**: Consistent behavior patterns
- **Feedback**: Clear validation states

### Content

- **Helpful**: Clear labels and placeholder text
- **Concise**: Brief, descriptive messaging
- **Contextual**: Appropriate for the input type

## Best Practices

### Labels and Placeholders

- Always provide clear labels
- Use placeholders for examples, not instructions
- Keep placeholder text concise
- Consider internationalization

### Validation

- Validate on blur for better UX
- Provide clear, actionable error messages
- Use appropriate validation states
- Consider real-time validation for complex fields

### Icons

- Use icons to clarify input purpose
- Ensure icons are accessible
- Don't rely solely on icons for meaning
- Keep icon usage consistent

### Responsive Design

- Ensure adequate touch targets
- Test on various screen sizes
- Consider mobile keyboard types
- Maintain readability at all sizes

## Examples

### Login Form

```tsx
function LoginForm() {
  const [credentials, setCredentials] = useState({ email: "", password: "" });
  const [errors, setErrors] = useState({});

  return (
    <div className="space-y-4">
      <GlassInput
        type="email"
        label="Email"
        placeholder="Enter your email"
        value={credentials.email}
        onChange={(e) =>
          setCredentials((prev) => ({ ...prev, email: e.target.value }))
        }
        leftIcon={<Mail className="w-4 h-4" />}
        state={errors.email ? "error" : "default"}
        message={errors.email}
        required
      />

      <PasswordInput
        label="Password"
        placeholder="Enter your password"
        value={credentials.password}
        onChange={(e) =>
          setCredentials((prev) => ({ ...prev, password: e.target.value }))
        }
        state={errors.password ? "error" : "default"}
        message={errors.password}
        required
      />
    </div>
  );
}
```

### Settings Form

```tsx
function ProfileSettings({ user }) {
  return (
    <div className="space-y-6">
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        <GlassInput label="First Name" defaultValue={user.firstName} required />

        <GlassInput label="Last Name" defaultValue={user.lastName} required />
      </div>

      <GlassInput
        type="email"
        label="Email Address"
        defaultValue={user.email}
        leftIcon={<Mail className="w-4 h-4" />}
        required
      />

      <GlassInput
        label="Company"
        defaultValue={user.company}
        leftIcon={<Building className="w-4 h-4" />}
      />

      <GlassInput
        label="Website"
        placeholder="https://"
        defaultValue={user.website}
        leftIcon={<Globe className="w-4 h-4" />}
      />
    </div>
  );
}
```
