---
title: "Installation"
description: "Install Liquidify in your React project"
---

# Installation

Choose your preferred package manager to install LiqUIdify.

## Package Managers

```bash
npm install liquidify
```

```bash
yarn add liquidify
```

```bash
pnpm add liquidify
```

```bash
bun add liquidify
```

## CSS Import

Import the CSS once at your app root:

```tsx
import "liquidify/styles";
```

## TypeScript

LiqUIdify is built with TypeScript and includes type definitions. No additional `@types` packages are needed.

## Next Steps

After installation, continue with the [Quick Start](/getting-started/quickstart) guide to set up your first component.
