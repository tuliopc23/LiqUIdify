---
title: "Installation"
description: "Install Liquidify in your React project"
---

import { SFSymbol } from "../components/SFSymbol";

# Installation

<div className="text-title-1 mb-6" style={{ color: "var(--ios-blue)" }}>
  Choose your preferred package manager to install LiqUIdify.
</div>

## <div className="flex items-center gap-3 text-title-2 mb-4"><SFSymbol name="cube-box" size="xl" color="var(--ios-purple)" />Package Managers</div>

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
  <div className="glass-surface p-4 rounded-xl">
    <div className="flex items-center gap-3 mb-3">
      <SFSymbol name="terminal" size="md" color="var(--ios-red)" />
      <span className="text-headline font-semibold">npm</span>
    </div>
    ```bash
    npm install liquidify
    ```
  </div>

{" "}
<div className="glass-surface p-4 rounded-xl">
  <div className="flex items-center gap-3 mb-3">
    <SFSymbol name="terminal-fill" size="md" color="var(--ios-blue)" />
    <span className="text-headline font-semibold">yarn</span>
  </div>
  ```bash yarn add liquidify ```
</div>

{" "}
<div className="glass-surface p-4 rounded-xl">
  <div className="flex items-center gap-3 mb-3">
    <SFSymbol name="speedometer" size="md" color="var(--ios-orange)" />
    <span className="text-headline font-semibold">pnpm</span>
  </div>
  ```bash pnpm add liquidify ```
</div>

  <div className="glass-surface p-4 rounded-xl">
    <div className="flex items-center gap-3 mb-3">
      <SFSymbol name="bolt-fill" size="md" color="var(--ios-yellow)" />
      <span className="text-headline font-semibold">bun</span>
    </div>
    ```bash
    bun add liquidify
    ```
  </div>
</div>

## <div className="flex items-center gap-3 text-title-2 mb-4"><SFSymbol name="paintbrush-fill" size="xl" color="var(--ios-teal)" />CSS Import</div>

<div className="glass-surface p-6 rounded-xl mb-6">
  <div className="flex items-center gap-3 mb-4">
    <SFSymbol name="doc-text-fill" size="lg" color="var(--ios-green)" />
    <span className="text-headline font-semibold">
      Import the CSS once at your app root:
    </span>
  </div>
  ```tsx import "liquidify/styles"; ```
</div>

## <div className="flex items-center gap-3 text-title-2 mb-4"><SFSymbol name="swift" size="xl" color="var(--ios-orange)" />TypeScript</div>

<div className="glass-surface p-6 rounded-xl mb-6">
  <div className="flex items-center gap-3 mb-4">
    <SFSymbol name="checkmark-circle-fill" size="lg" color="var(--ios-green)" />
    <span className="text-headline font-semibold">Built with TypeScript</span>
  </div>
  <p className="text-body" style={{ color: "var(--apple-label-secondary)" }}>
    LiqUIdify is built with TypeScript and includes comprehensive type
    definitions. No additional `@types` packages are needed.
  </p>
</div>

## <div className="flex items-center gap-3 text-title-2 mb-4"><SFSymbol name="arrow-right-circle-fill" size="xl" color="var(--ios-blue)" />Next Steps</div>

<div className="glass-ultra-thin p-6 rounded-xl">
  <div className="flex items-center gap-3 mb-3">
    <SFSymbol name="play-circle-fill" size="lg" color="var(--ios-blue)" />
    <span className="text-headline font-semibold">
      Ready to start building?
    </span>
  </div>
  <p className="text-body" style={{ color: "var(--apple-label-secondary)" }}>
    After installation, continue with the{" "}
    <a
      href="/getting-started/quickstart"
      className="text-ios-blue hover:underline"
    >
      Quick Start
    </a>{" "}
    guide to set up your first component.
  </p>
</div>
