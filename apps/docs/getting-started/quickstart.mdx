---
title: "Quick Start"
description: "Get up and running with LiqUIdify's liquid glass components in under 5 minutes."
icon: "rocket"
---

# Quick Start Guide

Get up and running with LiqUIdify’s liquid glass UI in minutes. Install, wrap your app with the provider, and drop in a component.

## 📦 Installation

```bash
bun add liquidify
```

## 🎨 Setup Provider

Wrap your app with `GlassUIProvider` and import the CSS once:

```tsx
import { GlassUIProvider } from "liquidify";
import "liquidify/styles";

function App() {
  return <GlassUIProvider>{/* your app */}</GlassUIProvider>;
}
```

## 🌊 Your First Liquid Glass Component

```tsx
import { GlassCard, GlassButton } from "liquidify";

export default function WelcomeCard() {
  return (
    <GlassCard variant="elevated" className="p-8 max-w-md mx-auto">
      <GlassCard.Header>
        <GlassCard.Title>Welcome to LiqUIdify</GlassCard.Title>
        <GlassCard.Description>
          Experience beautiful liquid glass morphism with Apple-inspired design.
        </GlassCard.Description>
      </GlassCard.Header>

      <GlassCard.Content>
        <p className="text-white/80 mb-6">
          Build stunning interfaces with smooth animations and visual depth.
        </p>
      </GlassCard.Content>

      <GlassCard.Actions>
        <GlassButton variant="primary" size="lg">
          Get Started
        </GlassButton>
        <GlassButton variant="ghost">Learn More</GlassButton>
      </GlassCard.Actions>
    </GlassCard>
  );
}
```

## 🎯 Button Variants

```tsx
import { GlassButton } from "liquidify";

export function ButtonShowcase() {
  return (
    <div className="flex gap-4 flex-wrap">
      <GlassButton variant="primary">Primary</GlassButton>
      <GlassButton variant="secondary">Secondary</GlassButton>
      <GlassButton variant="ghost">Ghost</GlassButton>
      <GlassButton variant="destructive">Destructive</GlassButton>
    </div>
  );
}
```

## 🌙 Theme Toggle

```tsx
import { UnifiedGlassProvider, useTheme, GlassButton } from "liquidify";

function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  return (
    <GlassButton
      onClick={() => setTheme(theme === "light" ? "dark" : "light")}
      variant="ghost"
    >
      {theme === "light" ? "🌙" : "☀️"} Toggle Theme
    </GlassButton>
  );
}
```

## 🚀 Next Steps

- Browse the full library in Components
- Customize styles in Theming
- Review accessibility best practices in Guides → Accessibility
