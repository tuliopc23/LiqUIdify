---
title: Command Palette
description: Spotlight-style command menu with fuzzy filtering.
---

import Preview from "./Preview";
import * as React from "react";
import { GlassCommand } from "liquidify";

# Command Palette

<Preview
  title="Example"
  code={`import { GlassCommand } from "liquidify";
const ITEMS = [
  { id: "new", label: "New File" },
  { id: "open", label: "Open..." },
  { id: "save", label: "Save" },
];

export default function Demo() {
  return <GlassCommand items={ITEMS} placeholder="Type a command" />;
}`}
  language="tsx"
>
  {(() => {
    
    const ITEMS = [
      { id: "new", label: "New File" },
      { id: "open", label: "Open..." },
      { id: "save", label: "Save" },
    ];
    
    function Demo() {
      return <GlassCommand items={ITEMS} placeholder="Type a command" />;
    }
    return <Demo />
  })()}
</Preview>

## Props

| Prop          | Type                         | Description        |
| ------------- | ---------------------------- | ------------------ |
| `items`       | `{id:string;label:string}[]` | Command entries    |
| `onSelect`    | `(id:string)=>void`          | Selection callback |
| `placeholder` | `string`                     | Input placeholder  |
