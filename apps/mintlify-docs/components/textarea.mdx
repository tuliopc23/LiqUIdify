---
title: Textarea
description: Multi-line text input with liquid glass styling.
---

import Preview from "./Preview";
import * as React from "react";
import { GlassTextarea } from "liquidify";

# Textarea

<Preview
  title="Example"
  code={`import { GlassTextarea } from "liquidify";
export default function Demo() {
  return <GlassTextarea placeholder="Write something..." />;
}`}
  language="tsx"
>
  {(() => {
    function Demo() {
      return <GlassTextarea placeholder="Write something..." />;
    }
    return <Demo />;
  })()}
</Preview>

## Props

| Prop       | Type               | Description      |
| ---------- | ------------------ | ---------------- |
| `value`    | `string`           | Controlled value |
| `onChange` | `(v:string)=>void` | Change handler   |
| `rows`     | `number`           | Row hint         |
